引言

随着企业数字化转型加速，云计算已成为基础设施的核心。然而，云上资产的暴露面扩大，也带来了新的安全挑战。云上攻防不仅涉及传统的漏洞利用，更包括对身份认证机制、云服务元数据、存储服务的攻击。本文聚焦两大关键领域：RAM（访问控制） 与核心云产品（弹性计算服务器ECS、云数据库RDS），从理论到实践，深度解析攻击者如何利用配置缺陷、AK/SK泄露及元数据服务进行横向移动与权限提升，并提出相应的防御策略。

---

第一部分：RAM控制与AK/SK安全

1.1 访问控制基础：用户、角色与授权

云平台的访问控制（RAM）用于管理身份和权限。核心概念包括：

· 用户：代表一个实体（如应用程序或人员），拥有长期的访问密钥（AK/SK）。
· 角色：一种虚拟身份，不具备永久密钥，而是通过策略授权后，由“可信实体”（如云服务、其他账号）扮演并获取临时凭证。
· 授权：通过策略定义允许或拒绝的操作，附加给用户或角色。

在攻防视角下，攻击者关注的是如何获取有效凭证，以及凭证所拥有的权限范围。

1.2 AK/SK：云资产的“万能钥匙”

访问密钥（AccessKey，简称AK）和秘密密钥（SecretKey，简称SK）相当于云主机的账号密码，可用于调用云API管理所有资源。AK/SK的创建渠道主要有两种：

· 基于用户创建：归属于特定RAM用户，权限由用户策略决定。
· 基于云账号创建：归属于整个云账号，拥有该账号下的全局管理权限，风险极高。

1.2.1 特征识别：一眼锁定云厂商

不同云厂商的AK前缀具有明显特征，快速识别有助于后续针对性利用：

云厂商 AK前缀特征
亚马逊云 AKIA
谷歌云 GOOG
微软Azure AZ
IBM云 IBM
甲骨文云 OCID
腾讯云 AKID
阿里云 LTAI
华为云 20位随机大写字母+数字
百度云 AK
京东云 JDC_
联通云 LTC
移动云 YD
中国电信云 CTC

示例：若在代码中发现LTAIxxxxx，可初步判定为阿里云AccessKey。

1.2.2 常见泄露渠道

· 前端源码/JS文件：开发人员将AK硬编码在网页JavaScript中，用于调用云资源。
· APP反编译/小程序反编译：移动应用或小程序未混淆的代码中直接包含密钥。
· 源码泄露/配置文件：如GitHub泄露、.env文件、配置文件未从版本库中移除。
· 日志/调试信息：程序运行时打印AK到日志中。

1.2.3 泄露后的利用工具

一旦获得AK/SK，攻击者会使用自动化工具探测其权限并实施控制。以下为常用工具（注意原CF项目已停止更新，社区涌现了新替代品）：

· lc：一款综合性的云环境利用工具，支持多云厂商的资源枚举、权限提升等。
· cloudsword：专注于云上AK/SK利用，提供命令行交互，可执行列举资源、创建后门等操作。
· cloudTools：集合了多种云服务探测与利用脚本。
· alicloud-tools：针对阿里云的利用工具集，可管理ECS、OSS等。

实践提示：使用此类工具前，请确保获得合法授权，仅在授权测试环境中使用。

---

第二部分：云服务器（ECS）攻防——元数据服务的风险

2.1 前提条件

攻击者要利用云服务器进行横向移动，通常需要满足以下任一条件：

· 目标ECS实例配置了实例角色（即RAM角色），该角色绑定了某些权限。
· 攻击者已获得某台云服务器的WebShell、命令执行权限，或发现了SSRF（服务端请求伪造）漏洞，可以访问内部元数据服务。

2.2 元数据服务：云服务器的“自白书”

元数据服务（Metadata Service）是云厂商提供给每个实例的内部HTTP服务，用于实例获取自身信息（如实例ID、网络配置、甚至临时访问凭证）。攻击者一旦能访问该服务，就可能获得关键信息，特别是临时凭证（STS Token）。

各大云厂商元数据地址如下：

云厂商 元数据服务地址
阿里云 http://100.100.100.200/latest/meta-data/
腾讯云 http://metadata.tencentyun.com/latest/meta-data/
华为云 http://169.254.169.254/latest/meta-data/
亚马逊云 http://169.254.169.254/latest/meta-data/
微软云 http://169.254.169.254/metadata/instance?api-version=2017-08-01
谷歌云 http://metadata.google.internal/computeMetadata/v1/

注意：各厂商对路径和请求头可能有特殊要求，如谷歌云需添加Metadata-Flavor: Google头。

2.3 利用案例

案例1：拥有WebShell，直接获取临时凭证

攻击者已通过漏洞获取某台ECS的WebShell（如上传一句话木马）。在服务器内部执行curl命令访问元数据：

```bash
# 以阿里云为例，先列出元数据根路径
curl http://100.100.100.200/latest/meta-data/

# 查看是否绑定了RAM角色
curl http://100.100.100.200/latest/meta-data/ram/security-credentials/

# 假设返回角色名“ecs-role”，则获取临时凭证
curl http://100.100.100.200/latest/meta-data/ram/security-credentials/ecs-role
```

返回的JSON中包含AccessKeyId、AccessKeySecret和SecurityToken。利用这些临时凭证，攻击者可调用云API执行该角色拥有的所有操作，例如创建后门实例、下载OSS数据等。

案例2：利用SSRF漏洞获取凭证

某Web应用存在SSRF漏洞，可请求任意URL。攻击者构造请求指向元数据地址：

```
GET /ssrf?url=http://100.100.100.200/latest/meta-data/ram/security-credentials/ecs-role
```

若应用未对内网IP做限制，将返回临时凭证。攻击者拿到凭证后，即可在外部通过云API接管该账号下的资源。

2.4 实际操作流程总结

1. 探测元数据可达性：利用漏洞或命令执行访问元数据地址。
2. 发现角色信息：访问/latest/meta-data/ram/security-credentials/获取绑定的角色名称。
3. 获取STS凭证：拼接角色名称路径，获取完整的临时凭证（包含AK、SK、Token）。
4. 外部利用：使用云厂商CLI或工具配置临时凭证，执行列举实例、创建用户等操作。

防御提示：云厂商已推出元数据服务v2版本（如阿里云ECS Metadata V2），需通过PUT方法获取令牌，有效防止SSRF直接读取。建议启用增强版元数据保护。

---

第三部分：云数据库（RDS）攻防

3.1 获取数据库账号密码

攻击者获取云数据库访问权限的首要途径是账号密码泄露。常见方式包括：

· 源码配置文件：应用部署时，数据库连接串通常写在.env、application.properties等文件中，一旦这些文件泄露（如GitHub、目录遍历漏洞），即可获得明文密码。
· WebShell读取配置：通过WebShell在服务器上查找配置文件。
· 爆破：虽然云数据库通常有复杂密码策略且易触发告警，但弱密码仍存在可能。

3.2 连接数据库

获得账号密码后，连接性取决于网络配置：

· 白名单开放&外网直连：如果数据库开启了外网访问地址，且IP白名单配置宽松（如0.0.0.0/0），攻击者可直接使用Navicat、DBeaver等客户端远程连接，进行数据窃取或勒索。
· 内网访问限制：大多数生产环境数据库只允许内网访问。此时，攻击者需要借助已攻陷的同内网服务器作为跳板：
  1. 在WebShell服务器上安装数据库客户端（如mysql、psql）本地连接。
  2. 或使用端口转发工具（如chisel、frp、EarthWorm）将内网数据库端口转发到攻击者本地，再通过本地客户端连接。

3.3 实践案例

假设攻击者通过某Java应用漏洞获得WebShell，并在/config/application.yml中发现：

```yaml
spring:
  datasource:
    url: jdbc:mysql://rm-bp1xxxxx.mysql.rds.aliyuncs.com:3306/mydb
    username: admin
    password: MyPass123
```

经扫描，该RDS实例未开放公网，但WebShell所在服务器与RDS同属一个VPC。攻击者在WebShell上安装mysql客户端并连接：

```bash
mysql -h rm-bp1xxxxx.mysql.rds.aliyuncs.com -u admin -pMyPass123
```

成功登录后，可导出敏感数据、添加后门账号等。

防御提示：云数据库应遵循最小权限原则，使用内网访问，严格配置白名单，并启用SSL加密连接。同时，建议使用云厂商提供的数据库审计功能监控异常查询。

---

总结与防御建议

攻击链回顾

1. 凭证获取阶段：通过代码泄露、SSRF元数据读取、弱口令爆破等方式获得AK/SK或数据库密码。
2. 权限探测阶段：利用工具探测凭证的有效范围和云服务资源。
3. 横向移动与控制：通过云API创建后门资源，或直接访问数据库窃取数据。

防御措施

· RAM管理：
  · 避免使用根账号AK，为不同应用创建最小权限的RAM用户。
  · 启用多因素认证（MFA）。
  · 定期轮换AK/SK，并使用云厂商的密钥泄露检测服务。
· 元数据保护：
  · 启用元数据服务的v2版本（需要令牌），阻断SSRF直接读取。
  · 对于ECS实例，非必要不授予RAM角色；若必须，则严格限制角色权限。
· 代码与配置安全：
  · 实施代码扫描，防止AK硬编码。
  · 将敏感配置托管于凭据管理系统（如AWS Secrets Manager、阿里云KMS凭据管家）。
· 网络隔离：
  · RDS等数据库禁止开放公网访问，使用安全组限制仅允许特定应用服务器IP访问。
  · 内网服务器间通信实施微隔离。
· 监控与响应：
  · 开启云平台的操作审计（如AWS CloudTrail、阿里云ActionTrail），监控异常的API调用。
  · 部署云安全态势管理（CSPM）工具，持续检测配置风险。

云上攻防是一场持续演变的博弈，只有深入理解攻击者的手法，才能在防御中做到有的放矢。希望本文能为安全从业者提供实用的攻防视角，助力构建更安全的云环境。
