引言

ThinkPHP作为国内最流行的PHP开发框架之一，凭借其简洁易用、功能丰富等特点，广泛应用于各类Web应用开发中。然而，任何框架都难以避免安全漏洞的出现。在ThinkPHP 5.x版本中，曾曝出多个高危漏洞，其中变量覆盖导致的文件包含漏洞以及参数组合拼接导致的SQL注入漏洞尤为典型。本文将深入分析这两个漏洞的产生原理，通过代码审计和实际复现，帮助开发者理解漏洞成因并掌握修复方法。

一、变量覆盖引发文件包含漏洞

1.1 漏洞背景与影响版本

该漏洞源于ThinkPHP 5.x中extract函数使用不当，导致恶意用户可通过特定参数覆盖模板变量，进而控制include语句包含恶意文件。根据GitHub上的版本对比，受影响版本主要为ThinkPHP 5.0.0至5.0.19（不含修复版本）。攻击者利用该漏洞可实现任意文件读取或远程命令执行。

1.2 原理分析

漏洞核心位于框架的视图层（View）处理逻辑中。ThinkPHP的视图类在渲染模板时，使用了extract函数将模板变量导入当前符号表。若攻击者能够控制传入的变量名，便可能覆盖掉系统关键变量，如view实例的属性，最终导致模板文件路径被篡改。

关键的调用链如下：

1. 控制器中调用fetch或display方法渲染模板。
2. 视图类think\View的fetch方法中，通过extract将变量数组展开。
3. 如果变量名与视图类内部属性（如data、engine等）冲突，则可能覆盖这些属性。
4. 后续在include模板文件时，使用被覆盖的属性值构造文件路径，导致任意文件包含。

其中，extract函数的默认行为是EXTR_OVERWRITE，即如果变量名冲突，会覆盖已有的符号表变量。这为攻击者提供了覆盖内部属性的机会。

1.3 代码审计

在thinkphp/library/think/View.php中，fetch方法关键代码如下：

```php
public function fetch($template = '', $vars = [], $replace = [], $config = [])
{
    // ... 省略 ...
    if ($vars) {
        extract($vars, EXTR_OVERWRITE); // 变量覆盖点
    }
    // ... 调用模板引擎渲染 ...
}
```

若攻击者能够控制$vars数组的键名，使其与视图类属性名相同（例如engine），则$engine属性将被覆盖。后续在加载模板文件时，会通过$this->engine获取模板引擎实例，若被覆盖为恶意对象，可能触发危险方法。

进一步跟踪，模板引擎的fetch方法最终会调用include包含文件，如果能够控制文件路径，则实现文件包含。

1.4 环境搭建与复现

环境要求：

· ThinkPHP 5.0.0 - 5.0.19
· PHP 5.6 / 7.0
· Web服务器（如Apache/Nginx）

复现步骤：

1. 下载受影响版本框架（如5.0.15），搭建应用。
2. 在application/index/controller/Index.php中创建测试控制器：

```php
<?php
namespace app\index\controller;

class Index
{
    public function index()
    {
        // 直接渲染模板，不传递任何参数
        return view();
    }
}
```

1. 创建模板文件application/index/view/index/index.html，内容任意（如<h1>Hello</h1>）。
2. 构造恶意请求，通过GET参数覆盖engine变量：
   ```
   http://localhost/index.php/index/index/index?engine=../../../../../../../../etc/passwd
   ```
3. 观察响应，若服务器返回/etc/passwd文件内容，则漏洞存在。

注意：实际利用需结合具体环境调整路径，并且需要PHP配置允许包含远程文件或存在可解析的PHP文件。更常见的利用方式是包含日志文件或上传文件。

1.5 修复方案

官方在5.0.20版本中修复了该漏洞，主要措施包括：

· 使用extract时改为EXTR_SKIP模式，避免覆盖已存在的变量。
· 增加对模板变量名的白名单校验。

开发者应尽快升级框架至最新版本，或手动修改视图类中的extract调用方式。

---

二、组合拼接引发SQL注入漏洞

2.1 漏洞背景与影响版本

ThinkPHP 5.0.15至5.0.18版本中，由于对数组参数处理不当，导致在构造SQL语句时拼接了未过滤的用户输入，造成SQL注入漏洞。攻击者可通过传递特定的数组参数，利用insert方法配合inc/dec操作实现报错注入，获取数据库敏感信息。

2.2 原理分析

漏洞出现在数据库查询构造器的insert方法中。ThinkPHP支持通过数组参数对字段进行自增（inc）或自减（dec）操作，但在拼接SQL语句时，直接将用户传入的字段值拼接进了SQL，而未进行参数化处理。

关键调用链：

1. 控制器调用insert方法插入数据，参数为数组。
2. 数组中的元素如果包含inc或dec关键字，框架会解析为自增/自减操作。
3. 在生成INSERT语句时，框架将用户提供的值直接拼接成SQL片段，如UPDATE table SET field = field + 1，但如果用户传入的inc参数中包含恶意的SQL语句，则直接拼接到+后面，导致注入。

例如，构造username[0]=inc&username[1]=updatexml(1,concat(0x7e,user(),0x7e),1)&username[2]=1，框架会误将updatexml(1,concat(0x7e,user(),0x7e),1)视为一个表达式，拼接到SQL中，触发报错注入。

2.3 代码审计

在thinkphp/library/think/db/Builder.php中，parseData方法负责解析插入数据。当检测到值为数组且包含inc、dec等关键字时，会调用相应的处理方法：

```php
protected function parseData($data, $options)
{
    // ... 省略 ...
    foreach ($data as $key => $val) {
        if (is_array($val) && !empty($val)) {
            switch (strtolower($val[0])) {
                case 'inc':
                    $val[1] = isset($val[1]) ? $val[1] : 1;
                    $result[$key] = '`' . $key . '`+' . $val[1]; // 直接拼接
                    break;
                case 'dec':
                    $val[1] = isset($val[1]) ? $val[1] : 1;
                    $result[$key] = '`' . $key . '`-' . $val[1];
                    break;
                // ... 其他操作 ...
            }
        } else {
            $result[$key] = $this->parseValue($val);
        }
    }
    return $result;
}
```

可见，$val[1]未经过滤直接拼接进SQL语句，导致SQL注入。

2.4 环境搭建与复现

环境要求：

· ThinkPHP 5.0.15 - 5.0.18
· MySQL数据库
· Web服务器

复现步骤：

1. 安装受影响版本框架，配置数据库连接，创建测试表，例如user表，包含id和username字段。
2. 在控制器中添加一个方法，接收参数并调用insert：

```php
<?php
namespace app\index\controller;
use think\Db;

class Index
{
    public function index()
    {
        $username = input('get.username/a'); // 接收数组参数
        Db::name('user')->insert(['username' => $username]);
        return 'ok';
    }
}
```

1. 构造恶意请求，触发报错注入：
   ```
   http://localhost/index.php/index/index/index?username[0]=inc&username[1]=updatexml(1,concat(0x7e,user(),0x7e),1)&username[2]=1
   ```
2. 观察返回的错误信息，应包含数据库用户名等敏感数据。

2.5 修复方案

官方在5.0.19版本中修复了该漏洞，主要修复方式：

· 对inc和dec操作中的数值进行严格过滤，仅允许数字。
· 引入参数化查询，避免直接拼接。

开发者应升级框架至安全版本，或手动修改Builder.php中的对应代码，增加数值校验。

---

三、总结与安全建议

本文详细分析了ThinkPHP 5.x中的两个高危漏洞：变量覆盖文件包含和数组拼接SQL注入。这两个漏洞均源于框架对用户输入的不当处理，展示了代码审计中常见的危险函数（如extract）和不安全的拼接操作可能带来的严重后果。

对于开发者而言，在开发过程中应遵循以下安全原则：

· 谨慎使用extract、parse_str等变量注册函数，除非能完全控制变量名或使用安全模式。
· 所有用户输入必须经过严格过滤，尤其是涉及到数据库操作、文件操作的地方。
· 保持框架和依赖的及时更新，关注官方安全公告。
· 定期进行代码审计和安全测试，尽早发现潜在漏洞。

通过深入理解漏洞原理，开发者不仅能够修复已知问题，更能培养安全编程思维，构建更加健壮的应用。

---

参考链接：

· ThinkPHP官方GitHub版本比较：https://github.com/top-think/framework/compare/
· PHP专题博客：https://xinyisleep.github.io/PHP%E4%B8%93%E9%A2%98/
· 断点调试配置：https://blog.csdn.net/m0_60571842/article/details/133246064

（注：本文所述漏洞均已修复，仅供安全研究和学习使用，请勿用于非法用途。）
