引言

在PHP应用程序安全审计中，变量覆盖漏洞是一种常见且危害严重的安全问题。它通常由于开发者不当使用某些PHP函数或特性，导致攻击者能够篡改程序中的关键变量，进而实现未授权访问、权限提升、文件包含甚至远程代码执行。本文将深入探讨PHP中导致变量覆盖的几种典型场景，并结合三个真实CMS的漏洞案例，从原理到利用进行详细分析，帮助安全人员更好地理解和防范此类漏洞。

第一部分：变量覆盖漏洞的原理与常见函数

1. 变量覆盖概述

变量覆盖漏洞是指攻击者通过构造特定输入，覆盖程序中原本已定义的变量值，从而改变程序逻辑。PHP作为一种动态语言，其变量注册机制较为灵活，如果开发者未对用户输入进行严格过滤或对变量作用域控制不当，就可能引入此类风险。

2. $$ 可变变量

PHP的可变变量允许将一个变量的值作为另一个变量的名称。例如 $$key 表示取 $key 的值作为变量名，并操作该变量。当攻击者能够控制 $key 的值时，就可以覆盖任意全局变量。

```php
$b = 3;
foreach (array('_COOKIE','_POST','_GET') as $_REQUEST) {
    foreach ($$_REQUEST as $_key => $_value) {
        $$_key = $_value;   // 危险操作
    }
}
echo $b;
```

上述代码遍历 $_GET、$_POST、$_COOKIE 中的键值对，并通过 $$_key 将其注册为变量。攻击者只需传入 ?b=2 即可覆盖变量 $b，使程序输出 2。

3. extract() 函数

extract() 函数将数组中的键值对导入到当前符号表作为变量。若未使用 EXTR_SKIP 等安全选项，它会覆盖已有的变量。

```php
$a = 'original';
extract($_GET);  // 若传入 a=attack，则 $a 被覆盖
echo $a; // 输出 attack
```

4. parse_str() 函数

parse_str() 通常用于解析 URL 查询字符串，默认行为是将解析出的参数注册为变量。

```php
$b = 3;
parse_str('b=2'); // 注册变量 $b = 2
echo $b; // 输出 2
```

5. import_request_variables() 函数

此函数将 GET、POST、Cookie 中的变量导入全局作用域（已在 PHP 5.4 中移除），同样容易造成变量覆盖。

第二部分：实战案例分析

案例一：DuomiCMS 的 $$ 变量覆盖导致未授权登录

漏洞定位
在 DuomiCMS 的 /include/common.php 中搜索 $$，发现如下代码：

```php
foreach (array('_COOKIE','_POST','_GET') as $_request) {
    foreach ($$_request as $_key => $_value) {
        $$_key = $_value;
    }
}
```

该代码将用户提交的 GET、POST、Cookie 数据直接注册为全局变量，且未做任何过滤。

利用点分析
后台登录验证位于 /admin/login.php，最终调用 check.admin.php 中的 keepUser() 方法，该方法将用户信息存入 $_SESSION：

```php
var $keepUserIDTag = "duomi_admin_id";
var $keepgroupidTag = "duomi_group_id";
var $keepUserNameTag = "duomi_admin_name";
$_SESSION[$this->keepUserIDTag] = $this->userID;
$_SESSION[$this->keepgroupidTag] = $this->groupid;
$_SESSION[$this->keepUserNameTag] = $this->userName;
```

由于 common.php 在每个页面加载时执行，且后续调用了 session_start()，攻击者可以通过覆盖 $_SESSION 变量来伪造登录状态。

攻击构造
访问任意页面，构造 URL 参数：

```
?_SESSION[duomi_admin_id]=1&_SESSION[duomi_group_id]=1&_SESSION[duomi_admin_name]=admin
```

$$_GET 会将 _SESSION 注册为变量，其值为数组 ['duomi_admin_id'=>1, ...]，从而直接覆盖全局的 $_SESSION 数组。之后程序验证 $_SESSION['duomi_admin_id'] 等值时，均返回攻击者设定的值，实现未授权登录后台。

修复建议

· 避免使用 $$ 直接注册用户输入。
· 如需注册变量，应限制允许的变量名范围，或使用 extract() 配合 EXTR_SKIP 选项。
· 对 $_SESSION 等重要超全局变量进行写保护，或在使用前重新初始化。

案例二：BeesCMS 的 extract 变量覆盖导致未授权访问

漏洞定位
BeesCMS 的 /includes/init.php 中存在以下代码：

```php
extract($_POST);   // 危险操作
extract($_GET);
```

该代码将 POST 和 GET 数据直接导入当前符号表，覆盖已有变量。

利用点分析
后台权限校验位于 /admin/admin.php，其依赖的 /admin/init.php 中定义了登录检查逻辑：

```php
if ($_SESSION['login_in'] != 1 || $_SESSION['admin'] != 1 || time() - $_SESSION['login_time'] > 3600) {
    // 跳转登录
}
```

攻击者若能控制 $_SESSION 的内容即可绕过验证。而 /includes/init.php 在多个页面中被包含，且执行了 session_start()，因此攻击者可以通过 extract 覆盖 $_SESSION 变量。

攻击构造
由于 extract($_POST) 在全局作用域执行，攻击者可以 POST 多维数组数据：

```
_SESSION[login_in]=1&_SESSION[admin]=1&_SESSION[login_time]=99999999999
```

PHP 会将 _SESSION[login_in] 解析为 $_POST['_SESSION']['login_in']。extract($_POST) 将数组键名作为变量名，因此创建了变量 $_SESSION（类型为数组），覆盖了原本的 $_SESSION 超全局变量。后续的权限检查将直接使用攻击者伪造的 SESSION 数据，从而进入后台。

修复建议

· 切勿对用户输入使用 extract() 且不加限制。
· 若必须使用，应指定 EXTR_SKIP 或 EXTR_PREFIX_ALL 避免覆盖。
· 对 $_SESSION 的操作应使用专用函数，避免直接变量覆盖。

案例三：Metinfo 的 $$ 变量覆盖导致文件包含

漏洞定位
Metinfo 的 /include/common.inc.php 中存在类似 DuomiCMS 的 $$ 注册代码：

```php
foreach (array('_GET','_POST','_COOKIE') as $_key) {
    if (isset($$_key)) {
        foreach ($$_key as $_k => $_v) {
            $$_k = $_v;
        }
    }
}
```

同样，用户输入被注册为全局变量。

利用点分析
在 /about/index.php 中，存在文件包含代码：

```php
require_once $module;
```

变量 $module 来自何处？继续追踪发现 /include/module.php 被包含，其中对 $module 进行了初始化，但存在一个判断：

```php
if($fmodule != 7) {
    $module = ...; // 正常赋值
} else {
    // 其他处理
}
```

攻击者需要控制 $fmodule 和 $module 两个变量。由于 $$ 变量覆盖，攻击者可以通过 GET 参数覆盖它们。

攻击构造
构造如下 URL：

```
/about/index.php?fmodule=7&module=../../../../etc/passwd%00
```

· fmodule=7 绕过了 if($fmodule!=7) 的条件，使得后续不会重新为 $module 赋值。
· module=../../../../etc/passwd%00 则覆盖了 $module 变量，最终 require_once 包含任意文件，导致文件读取漏洞。

修复建议

· 严格过滤文件路径，禁止包含用户可控的路径。
· 禁用 allow_url_include，防止远程文件包含。
· 避免使用 $$ 直接注册变量，可采用白名单方式只允许特定变量注册。

结论与通用防范措施

变量覆盖漏洞通常源于开发者对用户输入的盲目信任，以及对PHP动态特性的滥用。通过上述案例可以看出，无论是 $$、extract() 还是 parse_str()，只要攻击者能够控制变量注册过程，就可能导致严重的安全后果。

通用防范措施：

1. 禁用危险函数：在 php.ini 中禁用 extract()、parse_str() 等可能引发覆盖的函数，或仅在受控环境下使用。
2. 初始化变量：在使用变量前确保其已被初始化，避免被用户输入覆盖。
3. 白名单过滤：若需注册用户输入，应使用白名单只允许预定义的变量名。
4. 使用超全局数组：直接使用 $_GET、$_POST 等超全局数组，而不是将其解包为独立变量。
5. 增强安全意识：在代码审计中重点关注 $$、extract、parse_str、import_request_variables 等函数，并结合动态调试验证漏洞。

变量覆盖漏洞虽小，但其危害往往能串联成攻击链，导致整个应用沦陷。只有深刻理解其原理，并在编码和审计中保持警惕，才能有效防御此类威胁。
