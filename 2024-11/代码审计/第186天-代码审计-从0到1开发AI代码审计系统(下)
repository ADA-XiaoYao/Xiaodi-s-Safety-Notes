## 1. 概述

本文是《从0到1开发AI代码审计系统》系列的第二部分。在上一部分中，我们探讨了系统的基础架构和核心概念。本文将聚焦于如何将业界领先的静态分析引擎 **CodeQL** 与 **大语言模型 (LLM)** 进行深度集成，并最终实现代码审计流程的自动化。

我们将通过自定义 `think-cell` 指令来封装和调用 CodeQL，实现从代码下载、数据库构建到安全分析的全流程自动化。随后，我们将设计并调用 AI 接口，利用其强大的代码理解和推理能力对 CodeQL 的扫描结果进行二次分析、总结和修复建议。最终，我们将整个流程整合到 Web 应用中，实现一键启动分析并通过前端展示结果，完成一个具备基础 AI 赋能能力的代码审计（AI-SCA）系统原型。

## 2. 技术原理

一个现代化的 AI 代码审计系统，其核心在于将传统静态应用安全测试（SAST）的精准性与 AI 的智能化分析能力相结合。

- **CodeQL 静态分析**: CodeQL 并非简单的文本匹配或正则搜索，它将源代码编译成一个可查询的数据库（CodeQL Database）。这个数据库以关系型数据的形式存储了代码的语法结构、控制流、数据流等信息。安全研究员可以通过编写 CodeQL 查询语言（QL）来精确地查找复杂的漏洞模式，例如“从用户输入到 SQL 执行引擎的数据流路径，且中途未经净化函数处理”。这种方式提供了极高的准确性和极低的误报率。

- **LLM 赋能分析**: 传统的 SAST 工具通常会输出大量的原始告警，需要安全工程师花费大量时间去甄别、理解和修复。LLM 在此环节扮演了“智能分析师”的角色。通过精心设计的 **Prompt**，我们可以让 AI 完成以下任务：
    - **结果聚合与降噪**: 对 CodeQL 输出的多个相似告警进行归类和总结。
    - **漏洞验证与解释**: 分析告警涉及的代码片段，判断其是否为真实漏洞，并用自然语言解释漏洞成因。
    - **提供修复建议**: 根据上下文生成具体的、可直接使用的修复代码。
    - **风险评级**: 结合漏洞类型和业务场景，对漏洞的严重性进行初步评估。

- **自动化流程**: 为了将上述两个核心能力串联起来，我们需要一个调度中心。在本文的实践中，我们使用 ThinkPHP 框架的自定义命令（Command）作为调度器，它负责按顺序执行代码拉取、CodeQL 数据库构建、CodeQL 分析、调用 AI 接口等一系列任务，将原本需要手动分步执行的操作封装成一条命令，实现“一键审计”。

## 3. 环境准备

在开始实战之前，请确保您的开发环境已具备以下工具和配置：

- **ThinkPHP 8.x**: 本项目后端开发框架。
- **CodeQL CLI**: 核心静态分析引擎。请从 [GitHub Releases](https://github.com/github/codeql-cli-releases/releases) 下载并配置好环境变量。
- **Git**: 用于从版本控制系统拉取源代码。
- **Maven/Gradle 等构建工具**: 如果分析 Java/Kotlin 项目，需要对应的项目构建工具来帮助 CodeQL 正确构建数据库。
- **AI 模型接口**: 一个可用的大语言模型 API 访问凭证，例如 DeepSeek、OpenAI GPT 系列、Kimi Chat 等。

## 4. 实战示例

以下是构建自动化 AI 代码审计系统的核心步骤和代码实现。

### 4.1. 自定义指令封装 CodeQL 调用

为了方便地从后端触发整个审计流程，我们创建一个自定义的 ThinkPHP 命令 `scan`。

1.  **创建命令文件**:
    使用 `think-cell` 脚手架快速生成命令类。

    ```bash
    php think make:command scan
    ```

2.  **注册命令**:
    在 `app/command.php` 文件中注册新创建的 `scan` 命令，使其能够被框架识别。

    ```php
    <?php
    // app/command.php
    
    return [
        // 将 'scan' 命令指向我们创建的 Scan 类
        'scan' => 'app\command\Scan', 
    ];
    ```

### 4.2. 实现 CodeQL 自动化调用逻辑

在 `app/command/Scan.php` 文件中，我们将实现完整的“下载 -> 创建 -> 分析”流水线。

```php
<?php
// app/command/Scan.php

namespace app\command;

use think\console\Command;
use think\console\Input;
use think\console\Output;
use think\facade\Db;

class Scan extends Command
{
    protected function configure()
    {
        $this->setName('scan')->setDescription('Start a full code audit process using CodeQL and AI.');
    }

    protected function execute(Input $input, Output $output)
    {
        $output->writeln("Code audit task started...");

        // 1. 从数据库获取待审计的项目列表
        $projectList = Db::table('project')->where('status', 1)->select()->toArray();
        if (empty($projectList)) {
            $output->writeln("No active projects to scan.");
            return;
        }

        $currentDir = getcwd(); // 获取当前工作目录

        foreach ($projectList as $project) {
            $output->writeln("Processing project: {$project['name']}");
            $repoUrl = $project['addr'];
            $repoHash = md5($repoUrl); // 使用 MD5 作为仓库的唯一标识
            $codePath = "/data/{$repoHash}"; // 源代码存储相对路径

            // 2. 下载或更新代码
            $output->writeln("Cloning repository from {$repoUrl}...");
            $cloneCmd = "cd data && git clone {$repoUrl} {$repoHash}";
            // 注意：实际生产中应处理已存在目录的情况，例如使用 git pull 更新
            shell_exec($cloneCmd);
            Db::table('project')->where('id', $project['id'])->update(['code_path' => $codePath]);

            // 3. 创建 CodeQL 数据库
            $output->writeln("Creating CodeQL database...");
            // 示例为 Java Maven 项目，--command 是关键，它告诉 CodeQL 如何编译项目
            $dbCreateCmd = "codeql database create {$currentDir}/CodeQL/{$repoHash} --language=\"java\" --command=\"mvn clean install -f {$currentDir}{$codePath}/pom.xml -Dmaven.test.skip=true\" --source-root={$currentDir}{$codePath} --overwrite";
            shell_exec($dbCreateCmd);

            // 4. 分析 CodeQL 数据库
            $output->writeln("Analyzing database with security queries...");
            $qlPackPath = "D:/path/to/your/codeql-repo/java/ql/src/Security"; // 替换为你的 CodeQL 查询库路径
            $outputPath = "{$currentDir}/CodeQL/{$repoHash}/{$project['id']}.sarif";
            $analyzeCmd = "codeql database analyze {$currentDir}/CodeQL/{$repoHash} {$qlPackPath} --format=sarif-latest --output={$outputPath}";
            shell_exec($analyzeCmd);

            // 5. (可选) 调用 AI 接口进行分析
            $output->writeln("Sending results to AI for analysis...");
            $sarifContent = file_get_contents($outputPath);
            $aiResult = $this->callDeepSeekAPI($sarifContent); // 假设的 AI 调用函数
            // 将 AI 的分析结果存入数据库或文件
            file_put_contents("{$currentDir}/results/{$project['id']}_ai_report.md", $aiResult);
        }

        $output->writeln("All audit tasks completed.");
    }

    /**
     * 调用 AI 接口处理 SARIF 结果
     * @param string $sarifData CodeQL 输出的 SARIF 格式数据
     * @return string AI 生成的分析报告
     */
    private function callDeepSeekAPI(string $sarifData): string
    {
        // 此处为伪代码，请根据您选择的 AI 服务商提供的 SDK 或 HTTP API 进行实现
        $apiKey = "YOUR_DEEPSEEK_API_KEY";
        $url = "https://api.deepseek.com/v1/chat/completions";

        $prompt = "你是一名资深的代码安全审计专家。请分析以下由 CodeQL 工具生成的 SARIF 格式的安全审计结果。请为每一个漏洞提供：1. 简洁的漏洞描述。2. 风险等级（高、中、低）。3. 详细的修复建议和代码示例。请以 Markdown 格式返回你的分析报告。\n\nSARIF 数据如下：\n" . $sarifData;

        // HTTP 请求逻辑...
        // $response = httpClient->post($url, ['headers' => ..., 'json' => ...]);
        // $aiContent = json_decode($response->getBody())->choices[0]->message->content;

        // return $aiContent;
        return "AI analysis result for the provided SARIF data."; // 模拟返回
    }
}
```

### 4.3. Web 端触发与结果展示

现在，我们需要在 Web 控制器中提供一个入口来异步触发 `scan` 命令，并提供一个页面来查看结果。

1.  **创建异步触发接口**:
    在控制器中创建一个 `audit` 方法，用于在后台执行 `php think scan` 命令。

    ```php
    <?php
    // app/controller/AuditController.php

    namespace app\controller;

    use app\BaseController;

    class AuditController extends BaseController
    {
        public function audit()
        {
            echo "Audit task has been started in the background...";

            $workDir = base_path(); // 获取应用根目录
            // 将标准输出和错误输出重定向到日志文件，并使用 `&` 实现后台执行
            $command = "cd /d \"{$workDir}\" && php think scan > audit.log 2>&1 &";

            // 使用 popen 启动后台进程，然后立即关闭句柄，不等待其完成
            pclose(popen($command, 'r'));

            // 可以重定向到状态页面或直接返回
            return redirect('/status'); 
        }
    }
    ```

2.  **创建结果展示接口**:
    提供一个 `info` 方法，用于读取 `audit.log` 日志文件或最终的 AI 分析报告，并在前端展示。

    ```php
    <?php
    // app/controller/AuditController.php (续)

    public function info()
    {
        $logFile = base_path() . 'audit.log';
        if (file_exists($logFile)) {
            // 使用 <pre> 标签可以保留文本的原始格式，包括换行和空格
            $logContent = file_get_contents($logFile);
            return '<pre>' . htmlspecialchars($logContent) . '</pre>';
        } else {
            return 'Log file not found. The task may not have started yet.';
        }
    }
    ```

3.  **前端页面**:
    在视图文件中，添加触发分析和查看详情的按钮。

    ```html
    <!-- 审计操作按钮 -->
    <a href="/audit" class="btn btn-sm btn-outline-danger">
        <i class="bi bi-search me-1"></i> 一键分析
    </a>

    <!-- 查看详情按钮 -->
    <a href="/info" class="btn btn-sm btn-outline-info" target="_blank">
        <i class="bi bi-info-circle me-1"></i> 查看日志
    </a>
    ```

## 5. 关键细节

- **后台执行与日志**: `> audit.log 2>&1 &` 是在 Linux/Unix-like 系统中后台执行命令的关键。`>` 将标准输出重定向到 `audit.log`，`2>&1` 将标准错误也重定向到标准输出（即同一个文件），`&` 则让命令在后台运行。在 Windows 中，可以使用 `start /B` 配合重定向。`pclose(popen($cmd, 'r'))` 是一种在 PHP 中执行后台命令的常用技巧。

- **CodeQL 的 `command` 参数**: 在创建数据库时，`--command` 参数至关重要。它模拟了项目的正常构建过程，使得 CodeQL 能够监控编译器的调用，从而捕获最准确的代码结构信息。对于非编译型语言（如 Python, JavaScript），通常可以省略此参数。

- **Prompt 工程**: AI 分析的质量高度依赖于 Prompt 的设计。一个好的 Prompt 应该包含：
    - **角色设定**: "你是一名资深的代码安全审计专家"。
    - **任务描述**: 清晰地说明需要 AI 做什么。
    - **上下文提供**: 将 CodeQL 的 SARIF 结果作为输入。
    - **格式要求**: "请以 Markdown 格式返回"，便于后续的渲染和展示。

- **代码美化展示**: 当从后端获取到 AI 返回的 Markdown 格式报告或代码时，前端可以使用 [Marked.js](https://marked.js.org/) 或类似库将其渲染成美观的 HTML 格式，并配合 [Highlight.js](https://highlightjs.org/) 实现代码块的语法高亮，提升可读性。

## 6. 安全总结

本文构建的 AI 代码审计系统原型，虽然基础但完整地展示了自动化 SAST 与 LLM 结合的巨大潜力。然而，在生产环境中部署此类系统时，必须考虑以下安全和优化点：

- **命令注入风险**: 所有 `shell_exec` 或 `popen` 中的变量（尤其是来自用户输入或数据库的）都必须经过严格的过滤和转义（如使用 `escapeshellarg()` 和 `escapeshellcmd()`），以防止命令注入攻击。
- **资源隔离**: 代码下载和 CodeQL 分析过程会消耗大量 CPU 和磁盘资源，且可能包含恶意代码。应在隔离的环境（如 Docker 容器）中执行这些操作，避免影响主应用服务器。
- **任务队列**: 对于耗时较长的审计任务，应使用专业的消息队列（如 Redis Queue, RabbitMQ）代替简单的后台进程。这能提供更好的任务管理、重试机制和状态跟踪。
- **增量分析**: 每次都全量分析仓库效率低下。CodeQL 支持对 Pull Request 进行增量分析，这在集成 CI/CD 流水线时是更优的选择。
- **AI 结果的可靠性**: LLM 可能会产生“幻觉”，即提供不准确或错误的分析结果。AI 的输出应作为安全工程师的辅助，而非最终决策依据。建立人工审核流程是保障审计质量的关键。

通过不断迭代和优化，这个系统可以演变为企业级软件供应链安全（SDL）体系中的一个强大工具。
