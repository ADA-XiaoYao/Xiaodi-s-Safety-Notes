## 1. 概述 (Overview)

在上一篇笔记中，我们探讨了利用主流关系型数据库（MySQL, MSSQL, Oracle）进行权限提升的经典方法。本篇将扩展我们的视野，聚焦于其他常见的第三方应用软件，这些软件因其配置不当、自身漏洞或凭证存储机制，同样可以成为 Windows 环境下权限提升的关键跳板。

我们将分为两大类进行探讨：
1.  **其他数据库及缓存服务**: 包括 PostgreSQL、Redis 和 Memcached。虽然其中一些在 Linux 环境下更为常见，但其提权思路具有普适性，且在跨平台场景中同样重要。
2.  **常用桌面及服务软件**: 包括远程控制软件、密码管理工具、FTP 服务、办公和社交软件等。这类提权的核心思路是信息搜集和凭证窃取。

本笔记旨在提供一个结构化的学习框架，帮助读者理解如何从一个低权限的立足点，通过利用这些第三方软件来横向移动或垂直提升权限。

## 2. 技术原理 (Technical Principles)

第三方应用软件提权的根本原理在于利用这些软件的**高权限服务**、**危险功能**、**安全漏洞**或**不安全的凭证存储机制**。

*   **服务型软件 (如 PostgreSQL, FileZilla Server)**: 这类软件通常会注册一个系统服务，并以高权限账户（如 `LocalSystem`, `NT AUTHORITY\SYSTEM`）运行。如果能通过漏洞或配置不当来控制这些服务执行任意代码，就相当于直接获取了系统最高权限。
*   **缓存与键值存储 (如 Redis, Memcached)**: 这类服务通常用于提升性能，但若缺乏访问控制，就会成为信息泄露的重灾区。攻击者可以连接服务，直接读取其中缓存的敏感数据（如 Session、Token、数据库凭证），或利用其文件写入功能实现代码执行。
*   **客户端与工具软件 (如 TeamViewer, Navicat, 浏览器)**: 这类软件为了方便用户，会在本地存储连接密码、会话凭证等敏感信息。当攻击者获得一个普通用户权限后，其首要目标就是翻找这些软件的配置文件或注册表项，解密并窃取已保存的凭证，从而冒用其他用户身份或连接到更高权限的系统。
*   **钓鱼与社会工程学**: 利用已控权限，在目标主机上植入经过伪装的恶意文件（如捆绑了后门的更新包、破解补丁），诱导高权限用户（如系统管理员）点击执行，从而反向获取其权限。

## 3. 环境准备 (Environment Preparation)

### 3.1 漏洞利用与连接工具

*   **PostgreSQL**:
    *   **漏洞详情**: CVE-2019-9193 是一个高危的任意代码执行漏洞，影响 PostgreSQL 9.3 到 11.2 版本。它允许具有 `COPY FROM/TO PROGRAM` 权限的攻击者执行任意操作系统命令。
    *   **复现环境**: 可以使用预置了漏洞环境的 Docker 镜像或云市场镜像进行快速搭建。
        *   参考镜像: `https://market.aliyun.com/products/56024006/cmjj016247.html`

*   **Redis**:
    *   **环境搭建**: Redis 在 Windows 上有官方支持的版本，但其提权技巧（如写 SSH 密钥、计划任务）主要适用于 Linux 目标。在 Windows 环境下，其主要风险在于未授权访问导致的信息泄露。
    *   **Linux 环境搭建示例**:
        ```bash
        # 安装编译工具
        yum groupinstall "Development Tools"
        yum install -y tcl
        # 下载、解压、编译
        wget http://download.redis.io/releases/redis-2.8.17.tar.gz
        tar xzf redis-2.8.17.tar.gz
        cd redis-2.8.17
        make
        # 启动服务 (注意：默认配置可能存在未授权访问风险)
        cd src
        ./redis-server
        ```

*   **Memcached**:
    *   **风险**: Memcached 本身不包含任何身份验证机制，任何能连接到其端口（默认为 11211）的客户端都可以自由读写数据。
    *   **复现环境**:
        *   参考镜像: `https://market.aliyun.com/products/56024006/cmjj017529.html`
    *   **服务管理命令 (Linux)**:
        ```bash
        # 开启服务
        systemctl start memcached.service
        # 关闭服务
        systemctl stop memcached.service
        # 查看状态
        systemctl status memcached.service
        ```

### 3.2 凭证搜集工具

在获得 WebShell 或桌面用户权限后，可以使用各类后渗透工具来自动化搜集凭证，例如：
*   **Mimikatz**: Windows 平台下最著名的凭证窃取神器。
*   **LaZagne**: 开源的、支持多种操作系统的密码恢复工具，能从大量常用软件中提取密码。
*   **SharpHound / BloodHound**: 用于分析 Active Directory 环境中的攻击路径和权限关系。

## 4. 实战示例 (Practical Examples)

### 4.1 PostgreSQL (CVE-2019-9193) 提权

此漏洞利用 `COPY FROM PROGRAM` 命令，将命令执行的输出结果作为数据源写入表中，从而实现了命令执行。

*   **前提条件**:
    1.  拥有一个数据库用户密码。
    2.  该用户拥有创建表（`CREATE`）和执行 `COPY` 命令的权限。
    3.  PostgreSQL 版本在 9.3 至 11.2 之间。

*   **攻击流程**:
    ```sql
    -- 1. 清理可能存在的旧表
    DROP TABLE IF EXISTS cmd_exec;

    -- 2. 创建一个用于存储命令执行结果的表
    CREATE TABLE cmd_exec(cmd_output text);

    -- 3. 利用 COPY FROM PROGRAM 执行系统命令（以 Linux 的 'id' 命令为例）
    -- 在 Windows 上，可以将 'id' 替换为 'whoami'
    COPY cmd_exec FROM PROGRAM 'whoami';

    -- 4. 查询表内容，获取命令执行结果
    SELECT * FROM cmd_exec;
    ```
    通过这种方式，攻击者可以执行任意系统命令，例如下载并执行恶意程序、添加用户等，从而将数据库权限提升为系统权限。

### 4.2 Redis & Memcached 信息泄露

虽然 Redis 的经典提权手法主要针对 Linux，但在 Windows 环境下，它和 Memcached 最大的威胁是**未授权访问导致的信息泄露**。

*   **场景**: 攻击者通过端口扫描发现目标服务器开放了 Redis (6379) 或 Memcached (11211) 端口，且没有配置任何密码或访问控制。

*   **攻击流程 (以 Memcached 为例)**:
    1.  **连接服务**: 使用 `telnet` 或 `netcat` 连接到目标服务。
        ```bash
        telnet <target_ip> 11211
        ```
    2.  **信息探测**: 发送 `stats items` 或 `stats slabs` 等命令，查看缓存中有哪些数据项。
    3.  **数据转储**: 遍历所有 `slab` 和 `item`，使用 `get <key>` 命令逐一读取缓存内容。
        ```
        # 示例：假设发现一个名为 "user:session:123" 的键
        get user:session:123
        # 服务器可能返回包含用户ID、角色、会话令牌等敏感信息的序列化字符串
        ```
    4.  **利用信息**: 攻击者可以利用获取到的 Session Token 冒充用户登录 Web 应用，或利用获取到的数据库连接字符串直接连接数据库。
    *   **案例参考**: [某App后端Memcached未授权访问漏洞导致大量用户数据泄露](https://mp.weixin.qq.com/s/V_p1heyM-2HxsaFLRs9qeg)

### 4.3 客户端软件凭证窃取

当攻击者通过 WebShell 获得一个低权限用户（如 `www-data`, `iusr`）或通过钓鱼获得一个普通桌面用户权限后，搜集本地存储的凭证是最高效的提权和横向移动手段。

*   **场景 1: 窃取 Navicat 密码**
    1.  **定位目标**: Navicat 将其连接信息加密存储在 Windows 注册表中。
    2.  **攻击流程**:
        *   攻击者在目标机器上执行脚本，或使用 LaZagne 等工具，自动扫描注册表路径 `HKEY_CURRENT_USER\Software\PremiumSoft\Navicat\Servers\`。
        *   读取该路径下所有连接配置的 `Host`, `Port`, `UserName`, `Pwd` 等键值。
        *   `Pwd` 字段的值是经过加密的。使用已知的 Navicat 加解密算法（通常是 Blowfish 或 AES）进行解密，即可还原明文密码。
        *   利用获取的数据库密码，尝试连接数据库进行下一步提权。

*   **场景 2: 窃取浏览器密码和 Cookie**
    1.  **定位目标**: 现代浏览器（Chrome, Firefox, Edge）将密码、Cookie 等数据存储在本地的 SQLite 数据库文件中（如 `Login Data`, `Cookies`）。
    2.  **攻击流程**:
        *   找到目标用户的浏览器配置文件目录（例如 `C:\Users\<Username>\AppData\Local\Google\Chrome\User Data\Default\`）。
        *   复制 `Login Data` (存储密码) 和 `Cookies` (存储会话) 文件。
        *   在 Windows 上，密码数据使用 DPAPI (Data Protection API) 进行了加密。攻击者需要**在当前用户会话下**运行解密工具，或者窃取用户的 DPAPI Master Key 来离线解密。
        *   解密后，攻击者可以获得大量网站的登录凭证，可能包含OA、VPN、云服务等高价值目标的入口。

*   **场景 3: 窃取 TeamViewer 密码**
    1.  **定位目标**: TeamViewer 的 ID 和密码（用于无人值守访问）信息存储在注册表中。
    2.  **攻击流程**:
        *   攻击者查询注册表项 `HKEY_LOCAL_MACHINE\SOFTWARE\WOW6432Node\TeamViewer`。
        *   部分版本的 TeamViewer 密码（特别是旧版本）可能以加密形式存储在 `SecurityPasswordAES` 等键值中。
        *   通过特定工具或脚本可以解密该密码，从而获得对该机器的远程控制权限。

## 5. 关键细节 (Key Details)

*   **权限上下文**: 凭证窃取类攻击的成败，关键在于你当前所处的**用户上下文**。例如，窃取 DPAPI 加密的数据，最简单的方式就是在目标用户的会话中执行解密代码。
*   **动态与静态密码**: 远程控制软件（如 TeamViewer）通常有动态密码和静态密码（用于无人值守）。攻击者的目标是获取后者。
*   **工具的隐蔽性**: 在目标主机上直接运行 Mimikatz 等知名工具极易被杀毒软件拦截。实战中通常使用经过免杀处理的变种、内存加载技术或更小众的定制化脚本。
*   **信息关联**: 单一的凭证可能价值有限，但将从浏览器、数据库客户端、FTP 客户端等多个软件中窃取到的信息进行关联分析，往往能拼凑出完整的攻击链。

## 6. 安全总结 (Security Summary)

防范基于第三方软件的提权攻击，需要从软件生命周期的各个阶段入手，并培养纵深防御的思维。

1.  **安全基线配置**:
    *   **数据库/缓存服务**: 部署时必须修改默认密码，配置强密码策略。禁止将服务暴露在公网上，并使用防火墙限制仅内网可信 IP 访问。对 Redis、Memcached 等服务启用认证功能。
    *   **客户端软件**: 避免在软件中保存高权限或敏感系统的密码。如果必须保存，应使用主密码等机制对凭证库进行二次加密。

2.  **最小权限原则**:
    *   运行服务的账户应是低权限的专用账户，而非 `SYSTEM` 或 `Administrator`。
    *   Web 应用运行在隔离的环境中（如容器、低权限用户），限制其对文件系统和注册表的访问。

3.  **终端安全加固**:
    *   部署现代化的 EDR (Endpoint Detection and Response) 解决方案，以检测和阻止 Mimikatz 等凭证窃取工具的运行。
    *   教育用户不要下载和运行来源不明的软件，警惕钓鱼攻击。

4.  **漏洞与补丁管理**:
    *   定期扫描并更新所有第三方软件（包括数据库、服务器软件、客户端工具）至最新版本，及时修复已知漏洞。

5.  **凭证管理**:
    *   在企业环境中，推广使用密码管理器（如 KeePass, 1Password, Bitwarden）和多因素认证（MFA），减少密码明文或弱加密存储的情况。

---
